# 事件任务对话压缩上下文

> 本文件由 AI 助手在任务执行后自动维护，用于跨会话/跨账号恢复任务上下文（节省积分）。
> 最后更新：2026-08-28

## 项目状态总览
- **产品**：小小下载器（Electron + Vue3 + Naive UI 前端 / Python gui_bridge.py 后端，NDJSON 协议）
- **发布**：`小小下载器/小小下载器.exe`（v1.1.0，electron-builder portable 105MB）
- **更新机制（新）**：`补丁.exe` 与本体同目录运行，覆盖更新后自删；**旧 update/ 文件夹方案已废弃**
- **代码备份**：GitHub 私有仓库 `secondashes/xiaoxiaodownloader`（凭据在本机 Windows 凭据管理器，git push 直接可用）；每次更新结束 git commit + push

## 已支持站点（10 个，竖排三类按钮）
- 二次元：pawchive(pw) / exhentai / iwara(tv+ai 双站) / hanime(H站) / oreno3d(O3D) / erommdtube(制作中) / asmr(制作中)
- 三次元：coomer(xxxcoomer.com)
- 综合类：bunkr / x(Twitter)

## 2026-08-28 进行中的任务（本轮）
1. ✅ GitHub 私有仓库同步建立（初始 495 文件已推送 main 分支）
2. ✅ 调研完成：oreno3d 完善（角色列表/作者列表/四排序/无服务端账号-收藏纯localStorage/相关视频 section.g-main-video-related/作者描述含MEGALink）、erommdtube（与oreno3d同库同路由不同选择器，缩略图同用oreno3d CDN）、asmr-100（Kikoeru系统，API api.asmr-200.com，JWT登录365天，音轨树/api/tracks/{id}?v=2，直链/api/media/download/{workId}/{fileId}无签名）
3. ✅ 后端实现：oreno3d 完善 + erommdtube + asmr-100（gui_bridge.py，命令全部注册，ORENO_SITES[erommdtube] 选择器映射已加，asmr 4 域名容灾 + JWT 自动重登 + 内置音频播放器）
4. ✅ 前端：搜索框站点化辅助提示（已存在，确认各 site placeholder）/ 预览默认静音+音量持久化（RightPanel.vue previewMuted/previewVolume + localStorage + 静音按钮+音量滑块）/ 翻译按钮接入有道智云 API（gui_bridge.translate_youdao + LeftPanel 完整翻译面板 + API 配置折叠）/ 设置-更新模块（check_github_update 命令拉取 secondashes/xiaoxiaodownloader commits/main + releases/latest，本地基准 sha 对比 has_update，LeftPanel 设置区底部检查更新按钮+信息展示+"我已更新"标记）
5. ✅ 补丁.exe 打包机制（patch_runtime.py + patch_runtime.spec + 打包补丁.bat）：PyInstaller onefile 内嵌 update/ 文件夹 → 单文件 补丁.exe（54MB），Tkinter GUI 无黑框显示进度，应用后 spawn cmd ping 延时 3 秒自删；本体 小小下载器.exe 跳过（被锁），失败项记录明细
6. ✅ 全站批量下载检查补全；EX/X 点开链接自动解析展示

## 2026-08-28 第 6 项完成（EX 自动解析 + 全站批量下载复核）
- **全站批量下载现状（已确认全覆盖）**：
  - Bunkr/Pawchive/Coomer 共用 fileList 视图：selectAll/selectNone/invertSelection/selectByType("ok") + 表格勾选 + 方格点击切换（line 655-658）
  - Pawchive 画师工具栏额外"批量解析全部"（line 929，拉画师全部帖子文件）
  - Iwara `iwBatchMode`（主页勾视频/关注好友勾用户）+ `iw-batch-download` 事件
  - Hanime1 `haBatchMode`（主页分区/用户中心/搜索）+ `hanime-batch-download`
  - Oreno3D/EroMMDTube `orBatchMode` + `or-batch-download`
  - ASMR `asmrBatchMode` + `asmr-batch-download`
  - EX/X：fileList 视图自动接管（点击卡片 → 自动解析 → fileList.length>0 触发共用 fileList 视图，自带全选/反选/仅选可下载/批量下载）
- **EX 自动解析展示（关键改动）**：
  - App.vue `handleExOpenGallery(url)`：原仅发 `exhentai_gallery_info`（取元数据），现并发 `exhentai_gallery_info` + `inspect`（解析全部图片直链）→ 元数据先到（detail 视图短暂可见）→ inspect_complete 后 fileList 接管，自动进入文件列表视图（带批量下载）
  - 同步设 `url.value` / `searchQuery.value` / `cameFromSearch=true`，"后退"按钮可见
  - RightPanel.vue EX detail 视图条件加 `&& fileList.length === 0`：fileList 加载后自动隐藏（保留 exGalleryDetail 状态）
  - 文件列表工具栏新增 `ℹ️ 画廊信息` 切换按钮（仅 site===exhentai && exGalleryDetail）+ 内联面板 `.ex-inline-info`：展示发布者/时间/语言/大小/页数/评分/收藏/分组标签（可点击搜索），默认展开
  - handleBackToSearch 同步清理 exGalleryDetail/paPostDetail/paArtistPosts/iwDetail/haDetail/orDetail（避免回退后被旧详情卡住看不到搜索结果）
- **X 现状（无需新增）**：
  - X 浏览模式（twFollowMode==='browse'）：点推文卡片已自动 emit `open-album` → handleInspect → fileList 接管（"X 点开链接自动解析展示"已天然满足）
  - X 搜索模式点用户卡：进入 user 视图保留 follow/unfollow/TA 的关注 等交互按钮，"解析 TA 的全部媒体"为显式按钮（保留人工触发，避免自动解析抢占用户视图的交互入口；用户可点"后退"在两视图间切换）
- 改动文件：App.vue、RightPanel.vue（dist/ 已重新构建：vite build 成功，1.7MB js）
- ⏳ 未做：单文件 EXE 重构建（npm run dist，需镜像环境变量，~5-10 分钟 100MB）

## 关键技术备忘
- asmr-100 API：登录 POST /api/auth/me {name,password}（勿带Authorization头）；热门 POST /api/recommender/popular {page,pageSize}（勿传空数组参数会400）；收藏 PUT /api/review {work_id,progress:"marked"}；四域名容灾 api.asmr-200.com/api.asmr.one/api.asmr-100.com/api.asmr-300.com
- erommdtube 与 oreno3d 路由参数完全一致（sort=hot|favorites|latest|popularity 对应 急上昇/高評価/新着/人気），选择器映射：卡片 a.main__list-link vs a.box.pop_separate；iwara外链 figure.show__figure>a vs a.video-watch-btn2
- oreno3d 角色列表 /characters 单页全量（人気+五十音静态内嵌）；作者列表 /authors 分页1919页；两站无服务端账号体系，收藏做本地（POST /api/get-movies-by-id-order-by-column 可批量取卡片）
- hanime1 搜索 API：api.hanime1.me/rapi/v7/search POST（备用升级路线）
- 测试账号：hanime1 与 asmr-100 均为 yuejiaxiaosi/yuejiaxiaosi
- Electron 构建：gui/ 下需设镜像环境变量（ELECTRON_MIRROR=npmmirror 等，见执行文档）；改 gui_bridge.py 后先跑 打包后端.bat
