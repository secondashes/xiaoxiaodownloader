# 事件任务对话压缩上下文

> 本文件由 AI 助手在任务执行后自动维护，用于跨会话/跨账号恢复任务上下文（节省积分）。
> 最后更新：2026-08-28（AP1 完成 + 企划调整）

## 项目状态总览
- **产品**：小小下载器（Electron + Vue3 + Naive UI 前端 / Python gui_bridge.py 后端，NDJSON 协议）
- **发布**：`小小下载器/小小下载器.exe`（v1.1.0，electron-builder portable 105MB）
- **更新机制（新）**：`补丁.exe` 与本体同目录运行，覆盖更新后自删；**旧 update/ 文件夹方案已废弃**
- **代码备份**：GitHub 私有仓库 `secondashes/xiaoxiaodownloader`（凭据在本机 Windows 凭据管理器，git push 直接可用）；每次更新结束 git commit + push

## 已支持站点（13 个，竖排三类按钮）
- 二次元：pawchive(pw) / exhentai / iwara(tv+ai 双站) / hanime(H站) / oreno3d(O3D) / erommdtube(E站) / asmr(ASMR-100)
- 三次元：coomer(xxxcoomer.com) / xhamster(jp) / pornhub(jp) / xvideos
- 综合类：bunkr / x(Twitter)
- 注：xhamster/pornhub/xvideos 当前仅"登录框架"占位（见下方 AP1），具体搜索/解析/下载业务待用户给出合规要求后再补全

## 2026-08-28 企划调整（重要）
- 用户要求：**不搜索/不使用违规内容，仅制作框架 app**。三个成人站（xhamster/pornhub/xvideos）只做"登录 + 区域切换 + 代理设置"框架，不接入实际搜索/解析/下载业务
- 原企划 AP2/AP3/AP4（三站业务接入）→ **暂时挂起**，等用户后续给出具体要求再启动
- 立即推进的方向：**P2 翻译重制**（复刻 Edge 浏览器插件免费翻译，输入自动识别→中文输出）+ **P3 设置功能**（自定义快捷键录入 + 不息屏 + 快速缩小到托盘 + 拟态模式）
- 基础适配：通用 webview OAuth 框架已就绪，未来可复用于任何合规站点的 X 站 OAuth 登录

## 2026-08-28 AP1 完成（webview OAuth 通用框架）
- **核心组件**：`WebviewLoginModal.vue`（新建）—— 通用 webview 登录弹窗，嵌入 `<webview>` 完成 OAuth 授权 + 人机验证 + cookie 抓取；带工具栏（后退/前进/刷新/主页/地址栏）+ 状态机（idle/loading/oauth/captcha/success/failed）+ 自动检测 URL 模式触发抓 cookie
- **main.cjs**：`SITE_SESSIONS` 注册表（xhamster/pornhub 共享 `persist:twitter` 让 X 站 OAuth 自动带 cookie，xvideos 独立 `persist:xvideos`）+ `setupSiteSession()` 设代理/放行权限 + 3 个 IPC：`site-get-cookies`（抓 cookie 串+判定登录态）/`site-set-proxy`/`site-set-cookies`（注入恢复）
- **preload.cjs**：`siteGetCookies` / `siteSetProxy` / `siteSetCookies` 暴露给前端
- **gui_bridge.py**：`_GENERIC_OAUTH_SITES = ("xhamster","pornhub","xvideos")` + `_generic_save_cookies`/`_generic_load_cookies`/`_generic_cookie_str`/`_generic_check_login`/`_generic_logout`（cookie 字符串加密存 theme_cache.dat）+ 命令分发 `*_set_cookies`/`*_check_login`/`*_logout`/`*_set_proxy` + `_emit_login_info` 加入三站 + DEFAULT_SETTINGS 加 `xhamster_proxy`/`pornhub_proxy`/`xvideos_proxy`
- **App.vue**：`xhamsterUser`/`pornhubUser`/`xvideosUser` 状态 + `wvLogin` reactive + `handleSiteOAuthLogin`/`handleSiteLoginSuccess`/`handleSiteLogout`/`handleSiteSetProxy`/`handleSiteCheckLogin` + `site_login_result` 事件处理 + 启动 silent check_login + `handleRefreshLogin` 扩展 + LeftPanel 绑定新 props/events
- **LeftPanel.vue**：`needsLogin`/`siteLoggedIn`/`loginSubtitle`/`siteUsername`/`handleSiteLogout` 全部扩展三站 + `SITE_URLS` 加三站（jp.xhamster.com / jp.pornhub.com / www.xvideos.com）+ `SITE_HELP_TIPS` 三站（已去敏化，xvideos 不再含账号密码）+ 未登录分支加：xhamster/pornhub "用 Twitter (X) 登录" 按钮 + xvideos 邮箱密码表单（含"在此装置上记住我"checkbox）+ 设置区三站代理输入
- **RightPanel.vue**：站点切换组 Coomer 后加三个 chip（xHamster/Pornhub/XVideos）
- **去敏化**：xvideos 帮助文本中账号密码已删除，改为"在左侧表单填入你的 XVideos 邮箱与密码"占位
- 改动文件：gui_bridge.py、gui/electron/main.cjs、gui/electron/preload.cjs、gui/src/App.vue、gui/src/components/LeftPanel.vue、gui/src/components/RightPanel.vue、gui/src/components/WebviewLoginModal.vue（新增）；dist/ 已重新构建（vite build 成功，1.7MB js）
- bugfile/ 已同步全部改动文件

## 后续任务清单（按优先级）

## 2026-08-28 P2 完成（翻译重制 - Google 免费端点 + 自选引擎）
- **核心**：替换有道为主翻译后端，默认走 Google 翻译免费端点（`translate.google.com/translate_a/single?client=at`，无需 API key，自动检测源语言）
- **后端**（gui_bridge.py）：
  - `translate_free(text, from_lang='auto', to_lang='zh-CN')` 函数：①Google 免费端点（client=at + dt=t/bd + 标准 UA）→ 解析嵌套数组取译文 + 检测源语；②失败回退 LibreTranslate 自建实例（settings.libretranslate_url + 可选 api_key）
  - 命令分发 `translate_free`（asyncio.to_thread 包装）
  - DEFAULT_SETTINGS 加 `translate_engine`（默认 `google_free`，可选 `libretranslate` / `youdao`）、`libretranslate_url`、`libretranslate_api_key`
  - 旧 `translate_youdao` 函数 + 命令保留（兼容用户已配的有道 key，引擎切换可用）
- **前端**：
  - App.vue：`handleTranslateFree` 根据 `settings.translate_engine` 选 `translate_free` 或 `translate_youdao` 命令；保留 `handleTranslateYoudao` 兼容旧 emit
  - LeftPanel.vue：
    - doTranslate 改 emit `translate-free`（保留 `translate-youdao` emit 名兼容）
    - 翻译面板标题改"翻译（免费 / 自选引擎）"
    - 引擎选择器（n-select 下拉）：Google 免费 / LibreTranslate / 有道
    - 不同引擎显示对应配置：Google 只需提示语；LibreTranslate 显示 URL + key 输入；有道显示 app_id/secret 输入
    - 翻译结果加"检测源语言 + 引擎"meta 行（trDetected / trEngine computed）
    - CSS 加 `.translate-result-meta` 样式
- **去敏化**：默认即"自动识别 → 中文"，无需任何 API key 即可工作；国内网络 Google 端点需代理（设置区已有通用代理配置）
- 改动文件：gui_bridge.py、gui/src/App.vue、gui/src/components/LeftPanel.vue；dist/ 已重新构建（vite build 成功，1.7MB js）
- bugfile/ 同步中（下一步）

## 后续任务清单（按优先级）

### P3: 设置功能完善（优先级高，下一步执行）
**目标**：自定义快捷键录入 + 不息屏 + 快速缩小 + 拟态
- **3.1 自定义快捷键设置**：设置区每条快捷键加小齿轮按钮 → 点击弹"按下录入"框（显示开始录入/完成，支持组合键，组合键用空格分隔显示）→ 完成保存
- **3.2 不息屏模式开关**：用 Electron `powerSaveBlocker` API，开关持久化到 settings
- **3.3 快速缩小快捷键**：点击后进程最小化到任务栏消失，缩进右下角托盘，悬浮框关闭（用 `Tray` + globalShortcut + BrowserWindow.hide/minimize）
- **3.4 拟态模式**：开启后用快捷键唤起，上传 txt/word/图片/pdf 等做"伪装面板"，任务栏显示拟态面板，右下角后台运行，不显示悬浮框
- **现状**：main.cjs 已有 floatWindow/downloadsWindow + 简单 tray，需扩展
- **要做**：
  1. main.cjs：加 powerSaveBlocker.start/stop IPC、加 Tray 上下文菜单、加 globalShortcut 注册/解绑、加拟态窗口
  2. LeftPanel.vue：加快捷键录入弹窗组件 + 设置区条目
  3. App.vue：状态持久化 + 快捷键触发回调
  4. preload.cjs：暴露 powerSaveBlocker / tray / globalShortcut 相关 IPC

### AP2-AP4: 三站业务接入（挂起，待用户要求）
- 待用户提供"合规使用场景"与"具体业务范围"后再启动
- 框架已就绪：登录/cookie 持久化/代理设置/区域切换 全部可用
- 启动方式：用户给出新要求 → 用 `_generic_check_login` 升级为实际 API 验证 → 加 search/parse/download 命令分发 → 前端搜索结果卡片 + 文件列表 + 批量下载

## 关键技术备忘
- asmr-100 API：登录 POST /api/auth/me {name,password}（勿带Authorization头）；热门 POST /api/recommender/popular {page,pageSize}（勿传空数组参数会400）；收藏 PUT /api/review {work_id,progress:"marked"}；四域名容灾 api.asmr-200.com/api.asmr.one/api.asmr-100.com/api.asmr-300.com
- erommdtube 与 oreno3d 路由参数完全一致（sort=hot|favorites|latest|popularity 对应 急上昇/高評価/新着/人気），选择器映射：卡片 a.main__list-link vs a.box.pop_separate；iwara外链 figure.show__figure>a vs a.video-watch-btn2
- oreno3d 角色列表 /characters 单页全量（人気+五十音静态内嵌）；作者列表 /authors 分页1919页；两站无服务端账号体系，收藏做本地（POST /api/get-movies-by-id-order-by-column 可批量取卡片）
- hanime1 搜索 API：api.hanime1.me/rapi/v7/search POST（备用升级路线）
- 测试账号：hanime1 与 asmr-100 均为 yuejiaxiaosi/yuejiaxiaosi
- Electron 构建：gui/ 下需设镜像环境变量（ELECTRON_MIRROR=npmmirror 等，见执行文档）；改 gui_bridge.py 后先跑 打包后端.bat
- webview OAuth 通用框架：WebviewLoginModal.vue + main.cjs SITE_SESSIONS + gui_bridge.py _GENERIC_OAUTH_SITES，未来接合规站点只需加 SITE_SESSIONS 条目 + 站点 help/URL 即可
- Electron Tray/powerSaveBlocker/globalShortcut 在 main.cjs 主进程注册，preload 暴露 IPC 给渲染进程调用
