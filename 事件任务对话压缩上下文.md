# 事件任务对话压缩上下文

> 本文件由 AI 助手在任务执行后自动维护，用于跨会话/跨账号恢复任务上下文（节省积分）。
> 最后更新：2026-08-28（bug 修复轮 + AP1/P2/P3 完整状态）

## 项目状态总览
- **产品**：小小下载器（Electron + Vue3 + Naive UI 前端 / Python gui_bridge.py 后端，NDJSON 协议）
- **发布**：`小小下载器/小小下载器.exe`（v1.1.0，electron-builder portable 105MB）
- **更新机制（新）**：`补丁.exe` 与本体同目录运行，覆盖更新后自删；**旧 update/ 文件夹方案已废弃**
- **代码备份**：GitHub 私有仓库 `secondashes/xiaoxiaodownloader`（凭据在本机 Windows 凭据管理器，git push 直接可用）；每次更新结束 git commit + push

## 已支持站点（13 个，竖排三类按钮）
- 二次元：pawchive(pw) / exhentai / iwara(tv+ai 双站) / hanime(H站) / oreno3d(O3D) / erommdtube(E站) / asmr(ASMR-100)
- 三次元：coomer(xxxcoomer.com) / xhamster(jp) / pornhub(jp) / xvideos
- 综合类：bunkr / x(Twitter)
- 注：xhamster/pornhub/xvideos 当前仅"登录框架"占位（见下方 AP1），具体搜索/解析/下载业务待用户给出合规要求后再补全

## 2026-08-28 企划调整（重要）
- 用户要求：**不搜索/不使用违规内容，仅制作框架 app**。三个成人站（xhamster/pornhub/xvideos）只做"登录 + 区域切换 + 代理设置"框架，不接入实际搜索/解析/下载业务
- 原企划 AP2/AP3/AP4（三站业务接入）→ **暂时挂起**，等用户后续给出具体要求再启动
- 立即推进的方向：**P2 翻译重制**（复刻 Edge 浏览器插件免费翻译，输入自动识别→中文输出）+ **P3 设置功能**（自定义快捷键录入 + 不息屏 + 快速缩小到托盘 + 拟态模式）
- 基础适配：通用 webview OAuth 框架已就绪，未来可复用于任何合规站点的 X 站 OAuth 登录

## 2026-08-28 AP1 完成（webview OAuth 通用框架）
- **核心组件**：`WebviewLoginModal.vue`（新建）—— 通用 webview 登录弹窗，嵌入 `<webview>` 完成 OAuth 授权 + 人机验证 + cookie 抓取；带工具栏（后退/前进/刷新/主页/地址栏）+ 状态机（idle/loading/oauth/captcha/success/failed）+ 自动检测 URL 模式触发抓 cookie
- **main.cjs**：`SITE_SESSIONS` 注册表（xhamster/pornhub 共享 `persist:twitter` 让 X 站 OAuth 自动带 cookie，xvideos 独立 `persist:xvideos`）+ `setupSiteSession()` 设代理/放行权限 + 3 个 IPC：`site-get-cookies`（抓 cookie 串+判定登录态）/`site-set-proxy`/`site-set-cookies`（注入恢复）
- **preload.cjs**：`siteGetCookies` / `siteSetProxy` / `siteSetCookies` 暴露给前端
- **gui_bridge.py**：`_GENERIC_OAUTH_SITES = ("xhamster","pornhub","xvideos")` + `_generic_save_cookies`/`_generic_load_cookies`/`_generic_cookie_str`/`_generic_check_login`/`_generic_logout`（cookie 字符串加密存 theme_cache.dat）+ 命令分发 `*_set_cookies`/`*_check_login`/`*_logout`/`*_set_proxy` + `_emit_login_info` 加入三站 + DEFAULT_SETTINGS 加 `xhamster_proxy`/`pornhub_proxy`/`xvideos_proxy`
- **App.vue**：`xhamsterUser`/`pornhubUser`/`xvideosUser` 状态 + `wvLogin` reactive + `handleSiteOAuthLogin`/`handleSiteLoginSuccess`/`handleSiteLogout`/`handleSiteSetProxy`/`handleSiteCheckLogin` + `site_login_result` 事件处理 + 启动 silent check_login + `handleRefreshLogin` 扩展 + LeftPanel 绑定新 props/events
- **LeftPanel.vue**：`needsLogin`/`siteLoggedIn`/`loginSubtitle`/`siteUsername`/`handleSiteLogout` 全部扩展三站 + `SITE_URLS` 加三站（jp.xhamster.com / jp.pornhub.com / www.xvideos.com）+ `SITE_HELP_TIPS` 三站（已去敏化，xvideos 不再含账号密码）+ 未登录分支加：xhamster/pornhub "用 Twitter (X) 登录" 按钮 + xvideos 邮箱密码表单（含"在此装置上记住我"checkbox）+ 设置区三站代理输入
- **RightPanel.vue**：站点切换组 Coomer 后加三个 chip（xHamster/Pornhub/XVideos）
- **去敏化**：xvideos 帮助文本中账号密码已删除，改为"在左侧表单填入你的 XVideos 邮箱与密码"占位
- 改动文件：gui_bridge.py、gui/electron/main.cjs、gui/electron/preload.cjs、gui/src/App.vue、gui/src/components/LeftPanel.vue、gui/src/components/RightPanel.vue、gui/src/components/WebviewLoginModal.vue（新增）；dist/ 已重新构建（vite build 成功，1.7MB js）
- bugfile/ 已同步全部改动文件

## 后续任务清单（按优先级）

## 2026-08-28 P2 完成（翻译重制 - Google 免费端点 + 自选引擎）
- **核心**：替换有道为主翻译后端，默认走 Google 翻译免费端点（`translate.google.com/translate_a/single?client=at`，无需 API key，自动检测源语言）
- **后端**（gui_bridge.py）：
  - `translate_free(text, from_lang='auto', to_lang='zh-CN')` 函数：①Google 免费端点（client=at + dt=t/bd + 标准 UA）→ 解析嵌套数组取译文 + 检测源语；②失败回退 LibreTranslate 自建实例（settings.libretranslate_url + 可选 api_key）
  - 命令分发 `translate_free`（asyncio.to_thread 包装）
  - DEFAULT_SETTINGS 加 `translate_engine`（默认 `google_free`，可选 `libretranslate` / `youdao`）、`libretranslate_url`、`libretranslate_api_key`
  - 旧 `translate_youdao` 函数 + 命令保留（兼容用户已配的有道 key，引擎切换可用）
- **前端**：
  - App.vue：`handleTranslateFree` 根据 `settings.translate_engine` 选 `translate_free` 或 `translate_youdao` 命令；保留 `handleTranslateYoudao` 兼容旧 emit
  - LeftPanel.vue：
    - doTranslate 改 emit `translate-free`（保留 `translate-youdao` emit 名兼容）
    - 翻译面板标题改"翻译（免费 / 自选引擎）"
    - 引擎选择器（n-select 下拉）：Google 免费 / LibreTranslate / 有道
    - 不同引擎显示对应配置：Google 只需提示语；LibreTranslate 显示 URL + key 输入；有道显示 app_id/secret 输入
    - 翻译结果加"检测源语言 + 引擎"meta 行（trDetected / trEngine computed）
    - CSS 加 `.translate-result-meta` 样式
- **去敏化**：默认即"自动识别 → 中文"，无需任何 API key 即可工作；国内网络 Google 端点需代理（设置区已有通用代理配置）
- 改动文件：gui_bridge.py、gui/src/App.vue、gui/src/components/LeftPanel.vue；dist/ 已重新构建（vite build 成功，1.7MB js）
- bugfile/ 同步中（下一步）

## 2026-08-28 P3 完成（设置功能：快捷键录入 + 不息屏 + 快速缩小 + 拟态模式）
- **3.1 自定义快捷键录入**：
  - LeftPanel 设置区"系统功能"分组下 4 条快捷键项（切换不息屏 / 快速缩小到托盘 / 切换拟态模式 / 切换悬浮窗）
  - 每条加 ⚙ 小齿轮按钮 → 弹"录入快捷键"模态框
  - 录入流程：点"开始录入" → 监听 document keydown → 捕获组合键 → 自动停止 → 点"完成"保存
  - 显示格式：`Ctrl+Shift+M` → "Ctrl + Shift + M"（空格分隔）
  - KeyboardEvent → Electron accelerator 转换：Ctrl/Shift/Alt/Super + KeyA/Digit1/F1/Space/Return 等
  - 至少要修饰键或非可打印字符（避免单字母触发）
  - Esc 不作快捷键
- **3.2 不息屏模式开关**：
  - main.cjs `powerSaveBlocker.start('prevent-display-sleep')` / `.stop(id)`
  - IPC：`prevent-sleep-start` / `prevent-sleep-stop`
  - 持久化到 `settings.prevent_display_sleep`
- **3.3 快速缩小到托盘**：
  - main.cjs `createTray()` / `destroyTray()`：仅在最小化时创建，主窗口显示时销毁
  - Tray 菜单：打开主界面 / 显示拟态面板 / 退出；双击托盘图标直接显示主界面
  - `quickMinimizeToTray()`：隐藏主窗口 + 关闭悬浮窗 + 隐藏下载管理器 + 创建托盘
  - 触发方式：①快捷键 `register-shortcut` action=quick_minimize；②按钮 IPC `quick-minimize`
- **3.4 拟态模式**：
  - main.cjs `showMimicWindow(filePath)` / `toggleMimicMode()` / `enterMimic-mode` IPC
  - 拟态窗口加载用户上传的 txt/word/pdf/图片 等，作为"伪装面板"显示在任务栏
  - 内置占位页 `gui/electron/mimic.html`（未上传文件时显示）
  - 触发：①快捷键 `toggle_mimic`；②按钮 `enter-mimic-mode` + 文件路径；③托盘菜单"显示拟态面板"
  - 系统文件对话框 IPC `select-mimic-file`（支持 txt/md/html/htm/pdf/doc/docx/png/jpg/jpeg/gif/bmp）
- **全局快捷键机制**：
  - main.cjs `shortcutMap`（action→accelerator）+ `shortcutCallbacks`（action→回调）
  - IPC `register-shortcut(action, accelerator)`：先注销旧的，再注册新的；空字符串=仅注销
  - 退出时 `unregisterAllShortcuts()` 清理
  - `before-quit` 加 powerSaveBlocker.stop / destroyTray / mimicWindow.close
- **前端联动**：
  - App.vue `handleShortcutChange(action, acc)`：updateSettings 持久化 + registerShortcut IPC
  - `handlePreventSleepChange(enabled)`：preventSleepStart/Stop IPC
  - `syncP3SettingsToMain()`：settings 加载完成后批量注册全部快捷键 + 应用不息屏
  - `setupShortcutTriggeredListener()`：监听 main→renderer 的 `shortcut-triggered` 事件，前端处理 toggle_prevent_sleep / toggle_float 两个动作（需改 settings）
  - onMounted 时调用 setupShortcutTriggeredListener；settings 事件触发时调用 syncP3SettingsToMain
- **DEFAULT_SETTINGS 新增**：`prevent_display_sleep`、`shortcut_toggle_prevent_sleep`、`shortcut_quick_minimize`、`shortcut_toggle_mimic`、`shortcut_toggle_float`、`mimic_file_path`、`mimic_enabled`
- 改动文件：gui/electron/main.cjs、gui/electron/preload.cjs、gui/electron/mimic.html（新增）、gui/src/App.vue、gui/src/components/LeftPanel.vue、gui_bridge.py；dist/ 已重新构建（vite build 成功，1.7MB js）
- bugfile/ 同步中（下一步）

## 2026-08-28 bug 修复轮（commit f7410e7）

### ASMR URL 修复
- LeftPanel.vue `SITE_URLS` 之前缺少 `asmr` 条目，"打开网站"按钮 fallback 到 bunkr.sk
- 已加 `asmr: 'https://asmr-100.com/popular'`（用户实际主页）

### iw/ai 站点切换同步 + 强校验防错
- **bug 1**：切换到 AI 站时，"打开网站"按钮还是 iwara.tv（LeftPanel SITE_URLS.iwara 写死）
- **修复**：LeftPanel `openSiteInBrowser()` 改为按 `props.iwSite` 动态返回 `iwara.ai` / `iwara.tv`
- **bug 2**：未切换 AI 站时，前端偶尔显示 AI 站内容（旧 in-flight 请求返回的数据混入）
- **修复**：App.vue 在 `iwara_home` / `iwara_follow_list` / `iwara_friend_list` / `iwara_video_detail` 4 个事件 handler 加 `event.site !== iwSite.value` 强校验，不匹配则丢弃 + console.warn + addLog
- **后端配套**：gui_bridge.py `iwara_follow_list` / `iwara_friend_list` / `iwara_video_detail` 错误路径补 `site` 字段（之前只在成功路径有）

### EX 点击进入本子 + 批量下载按钮
- **bug**：点击 EX 搜索结果图片/名称不能进入画廊（但 tag 可点击）
- **根因**：`handleExOpenGallery(url)` 函数参数名 `url` 遮蔽（shadow）了外层 `url` ref，导致 `url.value = url` 自赋值 + 后续 `inspect` 命令把 ref 对象当 URL 字符串发送
- **修复**：参数改名为 `galleryUrl`，所有引用同步改名
- **新增**：EX 搜索结果工具栏加"批量下载选中 (n)"按钮
  - RightPanel.vue: 加按钮 + 新 emit `ex-batch-download`
  - App.vue: `handleExBatchDownload(urls)` 顺序派发 `inspect` 命令（间隔 1.5s 防 509），结果通过 `inspect_complete` 累加到 fileList
  - `exBatchDownloading` + `exBatchPending` 双状态：前者标记模式开启，后者计数异步返回的 inspect_complete（计数归零才关闭模式）
  - `inspect_complete` handler 双分支：批量模式 push 进 fileList；非批量模式保持替换

### 启动登录静默化 + 统一提示
- **bug**：打开程序时各站 check_login 弹独立提示，用户感觉来回切换
- **修复**：所有启动 `*_check_login` 都带 `silent: true`（exhentai/twitter 之前没有），并加一条统一 `message.loading('正在后台检查各站登录状态...')` 4 秒消失
- 注：login_result handler 已有 `event.silent` 分支不会弹成功/失败消息；只是之前启动没传 silent

### 构建
- ✅ 前端 dist/ 重新构建（vite build 成功，1.7MB js）
- ✅ Python 后端重新 PyInstaller 打包 → `bunkr_bridge/`（91.8MB clean）
- ✅ 单文件 EXE：`小小下载器/小小下载器.exe`（100.7MB）
- ✅ 补丁.exe：`补丁.exe`（56MB，bugfile/ 内嵌）
- 注：本轮 push 因 GitHub SSL 连接重置失败（network issue），commit f7410e7 在本地，下次网络好时手动 push 即可

## 后续任务清单

### oreno3d 站服务端登录 + 收藏同步（挂起，用户反馈"没有登录和保存"）
- **现状**：本地收藏已完整（`cache/oreno_favorites.json` + `or-favorites` 按钮 + 卡片 ♥），且工具栏已有"♥ 我的收藏"按钮
- **用户期望**：服务端账号登录 + 跨设备收藏同步
- **要做**：调研 oreno3d.com 是否有用户登录体系（注册/登录/收藏 API）；如有则仿照 hanime/x 站类型加 webview 登录 + cookie 持久化 + 服务端收藏拉取
- **优先级**：中（本地收藏已能满足基本需求）

### 性能优化（用户反馈"整体运行速度有些卡顿"）
- **现状**：之前已做下载进度节流 + snapshot 防抖 + JSON roundtrip 深拷贝
- **要做**：①profile 找具体卡顿场景（搜索结果渲染/缩略图加载/切换站点）；②vite 配置 manualChunks 拆包减小首屏 js（当前 1.7MB）；③大量 fileList 时虚拟滚动（如 EX 批量下载几百图）；④reactive 对象避免深 watch
- **优先级**：中

### AP2-AP4 三站业务接入（挂起，待用户合规要求）

### 最终构建（已完成 2026-08-28）
- ✅ Python 后端：`python -m PyInstaller gui_bridge.spec --noconfirm --clean` → `bunkr_bridge/`（91.8MB，clean）
- ✅ 单文件 EXE：`npm run dist`（ELECTRON_MIRROR=npmmirror）→ `小小下载器/小小下载器.exe`（101.4MB）
- ✅ 补丁.exe：`python -m PyInstaller patch_runtime.spec --noconfirm --clean` → `补丁.exe`（55.5MB）
- ✅ patch_runtime.spec 改用 `('bugfile', 'update')` 内嵌 bugfile/ 内容（旧 update/ 方案废弃，commit 1f2a469）
- ✅ bugfile/ 已同步全部改动文件（含 bunkr_bridge/ + dist/ + 源码 + mimic.html + 事件任务对话压缩上下文.md）
- 注：bunkr_bridge/ 与 *.exe 与 小小下载器/ 与 bugfile/ 均在 .gitignore 中，不入 GitHub；git 仅追踪源码改动
- 本次全部 commit：156163b（AP1）→ 552880e（P2）→ 5026581（P3）→ 1f2a469（patch_runtime.spec）

### AP2-AP4: 三站业务接入（挂起，待用户要求）
- 待用户提供"合规使用场景"与"具体业务范围"后再启动
- 框架已就绪：登录/cookie 持久化/代理设置/区域切换 全部可用
- 启动方式：用户给出新要求 → 用 `_generic_check_login` 升级为实际 API 验证 → 加 search/parse/download 命令分发 → 前端搜索结果卡片 + 文件列表 + 批量下载

## 关键技术备忘
- asmr-100 API：登录 POST /api/auth/me {name,password}（勿带Authorization头）；热门 POST /api/recommender/popular {page,pageSize}（勿传空数组参数会400）；收藏 PUT /api/review {work_id,progress:"marked"}；四域名容灾 api.asmr-200.com/api.asmr.one/api.asmr-100.com/api.asmr-300.com
- erommdtube 与 oreno3d 路由参数完全一致（sort=hot|favorites|latest|popularity 对应 急上昇/高評価/新着/人気），选择器映射：卡片 a.main__list-link vs a.box.pop_separate；iwara外链 figure.show__figure>a vs a.video-watch-btn2
- oreno3d 角色列表 /characters 单页全量（人気+五十音静态内嵌）；作者列表 /authors 分页1919页；两站无服务端账号体系，收藏做本地（POST /api/get-movies-by-id-order-by-column 可批量取卡片）
- hanime1 搜索 API：api.hanime1.me/rapi/v7/search POST（备用升级路线）
- 测试账号：hanime1 与 asmr-100 均为 yuejiaxiaosi/yuejiaxiaosi
- Electron 构建：gui/ 下需设镜像环境变量（ELECTRON_MIRROR=npmmirror 等，见执行文档）；改 gui_bridge.py 后先跑 打包后端.bat
- webview OAuth 通用框架：WebviewLoginModal.vue + main.cjs SITE_SESSIONS + gui_bridge.py _GENERIC_OAUTH_SITES，未来接合规站点只需加 SITE_SESSIONS 条目 + 站点 help/URL 即可
- Electron Tray/powerSaveBlocker/globalShortcut 在 main.cjs 主进程注册，preload 暴露 IPC 给渲染进程调用
