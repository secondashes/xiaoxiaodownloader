aiohttp==3.14.3
beautifulsoup4==4.15.0
Requests==2.34.2
rich==15.0.0
curl_cffi==0.16.2
# Pixiv 小说 word(.docx) 下载（下载器内 import；缺失时仅 word 选项不可用，txt 不受影响）
python-docx==1.2.0
# 识图（反向图片搜索）可选：用于大图自动压缩/缩放以适配各站上传限制
# 缺失时功能仍可用，只是超限图片会跳过对应站点并提示
Pillow
# 手动抓取·透明重定向（抓 QQ/微信等不走系统代理的应用；WinDivert 驱动需管理员运行）
pydivert>=3.1.3
