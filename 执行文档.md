# BunkrDownloader GUI 执行文档

> 八站点媒体下载器（Bunkr / Coomer / Pawchive / ExHentai / X / Iwara / Hanime1 / Oreno3D）的完整功能、要点、易错与改进思路总览。
> 双击 `启动.bat` 运行；分享更新时把 `update/` 文件夹内容覆盖到根目录即可。

---

## 〇·十、更新记录 · 2026-08-29（v1.2.11：1.2.10 崩溃修复 + 四站登录统一为 EX 式浏览器登录）

### 功能说明
1. **修复 1.2.10 分享版"无法运行"**：用户在 iwara 下载任务进行中切换站点时后端进程被杀（exit code 1），之后所有操作报"无法发送命令: pythonProcess=false"。根因：`src/url_utils.py` 的 `check_url_type` 对非 Bunkr URL 调用 `sys.exit(1)`，`SystemExit` 穿透 asyncio 事件循环静默杀死整个后端。
2. **X / Xh / Por / Xv 四站登录统一为 EX 站式"打开浏览器登录"**：左侧登录区改为与 EX 站完全相同的操作——点"打开浏览器登录（推荐）"弹出内置浏览器 → 用户任意方式登录（邮箱密码 / Google / Twitter 授权皆可）→ 点弹窗下方"确认"自动抓取 Cookie → 后端保存并验证。X 站原有的 auth_token/ct0 手动填写表单和"一键抓取浏览器 Cookie"入口已删除（保留粘贴整段 Cookie 的回退输入框）。

### 实现细节
- **崩溃修复（三层防护）**：
  - `src/url_utils.py`：`check_url_type` / `get_album_id` 由 `sys.exit(1)` 改为抛 `ValueError`；`downloader.py` CLI 捕获后非零退出
  - `gui_bridge.py` `_run`：新增站点守卫——任务 URL 非 pawchive/coomer/twitter 时，按文件 `site` 字段分流，非 bunkr 站点跳过 `check_url_type`（这些站下载走各自独立函数，无需抓任务 URL 页面）
  - `gui_bridge.py`：命令分发循环 / 任务处理 / 主循环三处捕获 `SystemExit`（src/ 残留 sys.exit 只记日志标记任务失败，不再杀进程）
- **四站 webview 登录**：
  - `gui/electron/main.cjs`：`SITE_SESSIONS` 注册 `twitter: { partition: 'persist:twitter', domains: ['.x.com', '.twitter.com'], authNames: ['auth_token'] }`
  - `gui/src/App.vue`：`handleSiteOAuthLogin` 新增 twitter 配置（x.com/login，手动确认模式）；xhamster/pornhub/xvideos 全部改 `manualConfirm: true`；`handleSiteLoginSuccess` 对 twitter 用 `cookies` 字段（后端 `twitter_set_cookies` 读取）；删除一键抓取的 twitter 分支
  - `gui/src/components/LeftPanel.vue`：X 站表单改为"打开浏览器登录（推荐）"按钮 + 整段 Cookie 粘贴回退；Xh/Por 删除"用 Twitter (X) 登录"改浏览器登录按钮；Xv 删除邮箱密码表单改浏览器登录按钮；一键抓取按钮排除 twitter
  - `gui/src/components/WebviewLoginModal.vue`：twitter 自身登录时不再误显"OAuth 授权中"状态
- 后端命令链路复用现有实现：`twitter_set_cookies`（解析 auth_token/ct0 + Viewer 验证）、`{site}_set_cookies` 通用保存 + `_generic_check_login`

### 剩余任务
1. xhamster/pornhub/xvideos 三站业务接入（登录已统一为浏览器登录，搜索/解析/下载待实现；登录验证目前仅判断 cookie 存在，用户名解析待接站 API）。
2. 用户实测：① iwara 下载中切换站点/搜索，后端不再退出；② X 站点"打开浏览器登录"→ 登录 x.com → 点确认 → 账号卡片显示用户名；③ Xh/Por/Xv 同流程。

### 产物（v1.2.11）
- 新单文件 EXE：`小小下载器/小小下载器 1.2.11.exe`
- 更新包：`补丁.exe`
- bugfile/ 已同步

---

## 〇·九、更新记录 · 2026-08-29（v1.2.10：点击反馈 + 预览优化 + 多线程下载）

### 功能说明
三项体验优化：
1. **点击立即反馈**：点击搜索结果卡片/画廊/解析按钮后，界面**瞬间**切换到"正在解析文件列表..."加载视图（大 spinner + 进度计数），不再无反馈让用户狂点；卡片/行按下时有缩放高亮反馈；新搜索（第 1 页）显示"正在搜索..."加载视图。
2. **预览优化**：图片预览弹窗加载原图时显示 spinner（不再白屏干等），加载完成淡入；加载失败显示明确错误提示（防盗链拦截时建议直接下载）；`decoding="async"` 解码不阻塞界面。
3. **多线程下载（IDM/迅雷式）**：≥8MB 文件自动多线程分段下载（原阈值 64MB 过高，绝大多数文件走单线程）；Range 支持探测升级为 HEAD + GET 双重探测（很多 CDN HEAD 不返回 Accept-Ranges 但 GET 支持，之前被误判为单线程）；默认下载线程数 4 → 8。

### 实现细节
- **App.vue**：新增 `beginInspectFeedback()`——点击瞬间乐观设置 `inspecting=true`（不等后端 `inspect_start` 事件）+ 120 秒看门狗（后端无响应时解除 loading 并报错）；`handleInspect`（搜索/历史/收藏/webview 解析）与 `handleExOpenGallery`（EX 点击画廊）均接入；`inspect_start`/`inspect_complete`/`inspect_error` 事件清除看门狗
- **RightPanel.vue**：
  - 搜索结果视图条件加 `&& !inspecting`（解析中让位）；新增 `inspect-loading-view`（`inspecting || searching` 时显示大 spinner + 动态文案）
  - 图片预览：`previewImageLoaded`/`previewImageFailed` 状态 + `watch(previewSrc)` 复位，img `@load`/`@error` 驱动，加载中 spinner 绝对定位居中
  - CSS：`.ex-tr:active` / `.search-card:active` 按压缩放 + 高亮（80ms）
- **src/config.py**：`MIN_PARALLEL_SIZE` 64MB → 8MB
- **src/downloaders/download_utils.py**：`detect_range_support` 升级——HEAD 拿到 Accept-Ranges 且有 Content-Length 时直接返回；否则发 `GET Range: bytes=0-0` 探测（HTTP 206 + Content-Range 确认支持并解析总大小；200 = 不支持）
- **gui_bridge.py**：`DEFAULT_SETTINGS.connections` 4 → 8；**注意**老用户 settings.json 已存 connections 值会覆盖默认，需在左侧「下载设置 → 下载线程数」手动调高（1-16）
- **LeftPanel.vue**：「并发连接数」改名「下载线程数（每文件多线程分段，类似 IDM/迅雷）」
- 并行下载核心（work-stealing 分段 + .partN 断点续传）此前已实现，本次打通触发条件让其实际生效

### 剩余任务
1. xhamster/pornhub/xvideos 三站业务接入（登录框架已通，搜索/解析/下载待实现）。
2. 用户实测：点击画廊瞬间显示"正在解析"（不再无反馈）→ 完成后进文件列表；下载大文件时任务管理器里 bunkr_bridge.exe 网络连接数明显增多（多线程生效）。

### 产物（v1.2.10）
- 新单文件 EXE：`小小下载器/小小下载器 1.2.10.exe`
- 更新包：`补丁.exe`
- bugfile/ 已同步

---

## 〇·八、更新记录 · 2026-08-29（v1.2.9：EX 站进入账号系统——真实用户名 + 多账号切换）

### 功能说明
EX 站 webview 登录抓到 cookie 后正式进入账号系统（与其他站点同模板）：账号卡片显示**真实用户名**（不再是数字 ipb_member_id），支持保存账号档案 / 多账号切换 / 删除档案 / 重新登录 / 退出登录，全链路打通。

### 实现细节（gui_bridge.py，纯后端改动）
- **真实用户名解析**：`_exhentai_check_login` 登录验证成功后从页面解析用户名——多模式匹配（IPB 论坛顶栏 "Logged in as: <a>" → showuser 个人资料链接 → 画廊站 "Hello <a>" → /u/<id>/<name> 用户页链接），失败再请求 forums.e-hentai.org 论坛首页兜底，全部失败回退 ipb_member_id；结果持久化为 cookie 存储里的 `_username`（下次免解析）
- **账号档案显示**：`_site_username('exhentai')` 优先返回 `_username`，`save_account` 保存档案时自动用真实用户名命名
- **多账号切换卫生**：`exhentai_set_cookies` 检测 `ipb_member_id` 变化（= 切换账号）时丢弃旧账号残留的 `_igneous` / `_username`（同一账号重新同步则保留 igneous 避免首请求 sadpanda）
- 账号系统链路（此前已通）：`_emit_login_info` 含 exhentai 条目 → 前端账号卡片（用户名/Cookie 复制/档案切换/保存/删除/重新登录/退出）；`switch_account('exhentai')` → `exhentai_set_cookies` 恢复档案并联网验证
- **违规内容跳过**：EX 账号体系只做登录/档案管理，站内违规内容相关功能暂不实现（如后续要加收藏同步等再评估）

### 剩余任务
1. xhamster/pornhub/xvideos 三站业务接入（登录框架已通，搜索/解析/下载待实现）。
2. 用户实测：webview 登录 EX → 账号卡片显示真实用户名（不是数字 ID）→「保存当前」建档 → 换号登录 → 下拉切换回旧档案。

### 产物（v1.2.9）
- 新单文件 EXE：`小小下载器/小小下载器 1.2.9.exe`
- 更新包：`update/` + `补丁.exe`
- bugfile/ 已同步

---

## 〇·七、更新记录 · 2026-08-29（v1.2.8：EX 站 webview 浏览器登录抓取 Cookie）

### 功能说明
EX 站登录不再依赖手动粘贴 cookie，改为弹出内置浏览器登录：
1. 左侧 EX 登录区新增「打开浏览器登录（推荐）」按钮
2. 弹窗打开 `https://forums.e-hentai.org/index.php?act=Login&CODE=00`（e-hentai 论坛登录页，EX 账号即论坛账号），会话复用 `persist:exhentai`（与右侧浏览器视图 cookie 互通）
3. 弹窗底部固定提示栏：「请登录，如果已登录请点击确认。」+ 右侧「取消」「确认」按钮
4. 用户登录完成（或已登录）后点「确认」→ 自动抓取 `.e-hentai.org` + `.exhentai.org` 域全部 cookie → 校验 `ipb_member_id`/`ipb_pass_hash` → 保存进账号系统

### 实现细节
- **main.cjs**：`SITE_SESSIONS` 注册 `exhentai`（partition 复用 `persist:exhentai`，domains 含两域）；`site-get-cookies` 新增 `authNames` 精确登录判定（按专属 cookie 名，替代通用数量启发式）
- **WebviewLoginModal.vue**：新增 `manualConfirm` 手动确认模式（EX 用）——不依赖 URL 自动检测（论坛登录成功后跳转地址不确定），底部提示+确认栏，隐藏顶部「完成抓取」按钮
- **App.vue**：`handleSiteOAuthLogin` 增加 exhentai 配置（manualConfirm 模式、代理用 exhentai_proxy）；登录成功后 cookie 同时以 `cookies` 字段发后端（`exhentai_set_cookies` 读该字段）
- **LeftPanel.vue**：EX 登录区加 webview 登录按钮 + 更新提示文案；原粘贴 cookie 方式保留作备选
- 代理自动生效：打开登录弹窗时把 `exhentai_proxy` 设置同步到 webview 会话

### 剩余任务
1. xhamster/pornhub/xvideos 三站业务接入（登录框架已通，搜索/解析/下载待实现）。
2. 用户实测 EX webview 登录全链路：打开浏览器 → 论坛登录 → 点确认 → 账号卡片显示已登录 → 搜索/下载正常。

### 产物（v1.2.8）
- 新单文件 EXE：`小小下载器/小小下载器 1.2.8.exe`
- 更新包：`update/` + `补丁.exe`
- bugfile/ 已同步

---

## 〇·六、更新记录 · 2026-08-29（v1.2.7：翻译转圈修复——快速失败 + 看门狗 + 默认不启动）

### 根因（"一直转圈"）
后端 `translate_batch` 尝试矩阵过大：Google 3域名 × 2代理配置 × 20s 超时 = 最长 2 分钟+，全失败后 MyMemory 逐条兜底（30条/批 × 15s超时 + 0.3s间隔）再拖几分钟；前端无超时机制 → 转圈永不停止。

### 修复内容
**后端快速失败（gui_bridge.py）**：
- `translate_batch`：Google 尝试压缩为最多 3 次 × 6s 超时（代理优先 googleapis → 直连 googleapis → 代理 translate.google.com）；MyMemory 兜底限量 20 条 × 5s 超时，连续 3 条失败（已限流）立即中止。最坏总时长 ~40s。
- `translate_free`：所有请求超时 15s→8s。

**前端（App.vue）**：
- **默认不启动**：启动时 `autoTranslateMode = false`（不再从 settings 恢复）；开关不再持久化（每次启动默认关，手动点击才开）。
- **45s 看门狗**：批量翻译发出后 45s 未返回强制停止转圈并提示（后端最坏 ~40s，覆盖全部场景）。
- **关闭立即生效**：关翻译时立即停转圈、清看门狗、代际计数 +1（在途请求的迟到结果丢弃不再回填）。
- **结果到达清看门狗**：translate_batch_result 处理时清超时定时器。

**RightPanel.vue**：
- 翻译按钮改自绘转圈（CSS 动画）——之前用 `:loading` 会禁用按钮，翻译中无法点击停止；现在转圈中仍可点击立即停止。

### 剩余任务
1. xhamster/pornhub/xvideos 三站业务接入（登录框架已通，搜索/解析/下载待实现）。
2. 用户实测：🌐 按钮点击开始（转圈）→ 再点立即停止；无代理环境 45s 内必有结果（译文或明确报错）。

### 产物（v1.2.7）
- 新单文件 EXE：`小小下载器/小小下载器 1.2.7.exe`
- 更新包：`update/` + `补丁.exe`
- bugfile/ 已同步

---

## 〇·五、更新记录 · 2026-08-29（v1.2.6：批量选择增强 + 清除批量任务 + 点画廊混入批量 bug）

### 本次完成
1. **EX 选择增强**：工具栏新增「全选 / 反选 / 取消全部」按钮（列表与缩略图模式勾选共用 exChecked，全站通用批量勾选入口）。
2. **清除批量任务按钮**：批量收集过文件后常驻工具栏（红色"清除批量任务 (N)"），一键清空收集的文件列表 + 复位批量状态（exBatchDownloading/exBatchPending/batchFileCollected）+ 解锁视图。**不影响已提交的下载任务和已下载文件**（下载状态页可查看）。批量解析进行中不可清除（防误触）。
3. **点画廊"弹回+增加批量任务"根因修复**（`handleExOpenGallery`）：
   - 批量解析进行中点击画廊：详情仍可浏览，但**不再发单个 inspect**——之前单个 inspect 的 inspect_complete 会被误计入批量计数（exBatchPending 错误递减导致批量提前结束）且文件混入批量收集列表（"点画廊增加批量任务"）。
   - 批量结束后点击画廊：重置 `batchFileCollected` 视图锁（之前锁永不复位——单个画廊解析完成后文件列表视图被隐藏，看起来"点击没反应/一直弹回搜索页"）。

### 关键实现位置
- `RightPanel.vue`：exCheckAll/exInvertCheck 方法；清除批量任务按钮（emit 'clear-batch-tasks'）；emits 声明。
- `App.vue`：handleClearBatchTasks（一键复位）；handleExOpenGallery 批量守卫 + batchFileCollected 重置；模板绑定 @clear-batch-tasks。

### 剩余任务
1. xhamster/pornhub/xvideos 三站业务接入（登录框架已通，搜索/解析/下载待实现）。
2. 用户实测：批量下载完成后点"清除批量任务"→ 浏览其他画廊应正常显示文件列表；反选/取消全部生效。

### 产物（v1.2.6）
- 新单文件 EXE：`小小下载器/小小下载器 1.2.6.exe`
- 更新包：`update/` + `补丁.exe`
- bugfile/ 已同步

---

## 〇·四、更新记录 · 2026-08-29（v1.2.5：翻译功能修复——dt 参数 bug + 多端点 + MyMemory 兜底）

### 根因（为什么"完全不可用，一直报错"）
1. **致命 bug**：`translate_free` 的请求参数 dict 里 `"dt": "t"` 与 `"dt": "bd"` 重复键——Python 后者覆盖前者，实际只发了 `dt=bd`（备选词），**没有 `dt=t`（翻译片段）**，Google 返回结构不含译文 → 永远报"格式异常"。
2. 单一端点 `translate.google.com` 国内被墙；无代理时必失败。

### 修复内容
1. **dt bug 修复**：只保留 `dt=t`（translate_batch 本来就没这个 bug）。
2. **多端点轮换**：`GOOGLE_TRANSLATE_URLS = [translate.googleapis.com, translate.google.com, clients5.google.com]`（googleapis 国内多数网络可直连），每个端点 × 代理/直连 依次尝试。
3. **MyMemory 免费接口兜底**（免配置，5000 字/天/IP）：
   - `translate_free`：Google 全失败后走 MyMemory，再走 LibreTranslate（用户配置），全链路兜底。
   - `translate_batch`：失败的批逐条走 MyMemory（0.3s 间隔防限流）。
   - 识别 MyMemory 限流告警文本（MYMEMORY WARNING）不算成功。
4. 有道翻译（translate_youdao）不受影响（API 参数本来就正确）。

### 翻译引擎链路（最终形态）
```
Google 免费端点（3 域名 × 代理/直连）
  ↓ 全失败
MyMemory 免费接口（免配置兜底）
  ↓ 失败
LibreTranslate（用户在左侧翻译面板自建配置）
  ↓ 失败
报错（前端显示具体原因）
```

### 剩余任务
1. xhamster/pornhub/xvideos 三站业务接入（登录框架已通，搜索/解析/下载待实现）。
2. 用户实测：翻译按钮（有代理时 Google 直出；无代理时 MyMemory 兜底也可用）。

### 产物（v1.2.5）
- 新单文件 EXE：`小小下载器/小小下载器 1.2.5.exe`
- 更新包：`update/` + `补丁.exe`
- bugfile/ 已同步

---

## 〇·三、更新记录 · 2026-08-29（v1.2.4：EX 下载失败走原站 reload broken image 兜底）

### 本次完成
**EX 图片下载失败兜底策略**（对应原站"图片加载失败点击刷新"功能）：
- 第 1 次正常解析直链下载；失败后的重试自动走原站 reload broken image（`?nl=token` 强制换 H@H 节点，服务器返回的通常是分辨率更低的重采样版本）。
- **兜底也失败才认定为下载失败**（进入失败清单 txt）；总尝试次数 = max(2, max_retries)，保证至少 1 次正常 + 1 次 reload 兜底（修复原 max_retries=1 时完全没有兜底的问题）。
- 进入兜底时下载日志提示"尝试原站 reload broken image 兜底，请求更低分辨率版本"；全部失败时提示"含 reload 兜底共 N 次尝试全部失败"。
- 附带修复：
  - 失败原因写入 item.error（失败清单 txt 第三列显示真实错误信息）；成功后清除。
  - 最终失败时清理残留的半截文件（流式下载中断可能留下部分内容，避免下次误判"已存在同名文件"）。

### 关键实现位置
- `gui_bridge.py`：`DownloadManager._exhentai_download_one._resolve_and_download()`（约 10348 行）——total_attempts 计算与兜底日志、final_path 半截文件清理、item.error 记录。

### 剩余任务
1. xhamster/pornhub/xvideos 三站业务接入（登录框架已通，搜索/解析/下载待实现）。
2. 用户实测验证：EX 批量下载失败图片自动走 reload 兜底；失败清单 txt 含真实错误信息。

### 产物（v1.2.4）
- 新单文件 EXE：`小小下载器/小小下载器 1.2.4.exe`
- 更新包：`update/` + `补丁.exe`
- bugfile/ 已同步

---

## 〇·二、更新记录 · 2026-08-29（v1.2.3：批量下载三大根因修复 + 本地缺失重下 + EX返回按钮）

### 本次完成
1. **批量只解析一个（根因修复）**：`inspect_start` 事件处理每次都执行 `fileList.value = []`——批量解析第 2/3/...个画廊时把之前收集的文件全部清空，最终只剩最后一个画廊的内容。修复：批量模式（exBatchDownloading/exBatchPending > 0）下 inspect_start 不清空文件列表、不置全局 loading。
2. **批量解析闪烁（根因修复）**：两个来源——① inspect_start 置 `inspecting=true` 触发全局 loading UI；② 第一个画廊解析完成后 fileList 从 0 变有 → 视图自动从搜索结果切到文件列表。修复：① 批量模式静默；② 新增 `batchFileCollected` 视图锁定（批量收集期间与完成后都不切换视图，完全静默后台下载；用户点返回/新解析/URL 解析时解锁）。
3. **EX 解析后无返回按钮**：返回按钮依赖 cameFromSearch，URL 栏直接解析不设置。修复：① handleInspect 也设 cameFromSearch=true；② 按钮条件扩展为 `cameFromSearch || (site==='exhentai' && exGalleryDetail)`。
4. **本地文件缺失重新下载（不再卡住）**：任务续传时 run_one 直接跳过 status=completed 的文件（即使本地文件已被删）。修复：completed 文件校验 `_final_path` 本地存在性，不存在 → 重置 pending 重新下载（done 计数同步扣减）。通用/Twitter 分支的"跳过重复"路径补写 `_final_path`（EX/Iwara/Hanime/Oreno3D/Asmr 已有）。
5. **缓存请求**：画廊解析已有缓存（cache/albums/，二次解析直接命中不请求站）；EX 下载时直链重新解析（keystamp 时效签名，缓存直链会过期）——保持现有设计。

### 关键实现位置
- `App.vue`：inspect_start 批量静默分支；batchFileCollected 锁定/解锁（confirmExBatchFolder 置、handleBackToSearch/handleInspect 解）。
- `RightPanel.vue`：文件列表视图条件 `!exBatchRunning && !batchFileCollected`；返回按钮条件扩展。
- `gui_bridge.py`：DownloadManager.run_one 本地存在性校验；通用/Twitter skip 分支补 `_final_path`。

### 剩余任务
1. xhamster/pornhub/xvideos 三站业务接入（登录框架已通，搜索/解析/下载待实现）。
2. 用户实测验证：勾选 10 个画廊批量下载（应全部收集+自动下载+不切视图）；删本地文件重试任务（应重新下载）。

### 产物（v1.2.3）
- 新单文件 EXE：`小小下载器/小小下载器 1.2.3.exe`
- 更新包：`update/` + `补丁.exe`
- bugfile/ 已同步

---

## 〇、更新记录 · 2026-08-29 凌晨（v1.2.2：缩略图多选 + 后台批量下载 + 失败清单 txt）

### 本次完成
1. **EX 缩略图模式多选下载**：缩略图卡片左上角新增勾选框（与列表模式共用 exChecked 数组），勾选后卡片蓝色高亮，工具栏自动出现"批量下载选中 (N)"按钮；两种显示模式勾选状态互通。
2. **批量下载完全后台化**：
   - 批量解析不再强制切换到文件列表视图——用户留在搜索结果页可继续浏览/翻页；
   - 解析结果静默收集，全部完成后**自动提交下载任务**（含母文件夹归档），提示"后台批量解析完成，已自动开始下载 N 个文件"；
   - 批量模式下 album_name 传空，让后端从文件列表取画师名作目录（避免错用最后一个画廊标题）；
   - 单画廊解析失败时同样递减批量计数（避免批量状态卡死），部分失败仍自动下载已收集的文件。
3. **下载失败清单 txt**：每个下载任务收尾时，若有失败文件，在下载目录生成 `下载失败清单_YYYYMMDD_HHMMSS.txt`（格式：文件名 | 网页链接 | 错误信息），UTF-8 BOM 编码记事本直接打开，日志区同步提示。适用于所有站点（DownloadManager 统一收尾处生成）。

### 关键实现位置
- `gui_bridge.py`：`DownloadManager._write_failure_report()`（约 10054 行）——任务收尾生成失败清单。
- `App.vue`：`confirmExBatchFolder()` EX 路径后台化（不设 cameFromSearch）；`inspect_complete/inspect_error` 批量自动提交（fromBatch 参数）；`handleDownload(selected, {fromBatch})`。
- `RightPanel.vue`：ex-thumb-card 勾选框 + `.ex-thumb-check` / `.ex-thumb-card-checked` 样式。

### 剩余任务
1. xhamster/pornhub/xvideos 三站业务接入（登录框架已通，搜索/解析/下载待实现）。
2. 用户实际运行验证：EX 缩略图勾选批量、后台批量下载全程不切视图、失败清单生成。
3. JavDB 真实登录流程验证（Cloudflare 验证 + cookie 抓取）。

### 产物（v1.2.2）
- 新单文件 EXE：`小小下载器/小小下载器 1.2.2.exe`（已移除 1.2.1）
- 更新包：`update/` + `补丁.exe`（已重新生成，内嵌本次全部改动）
- bugfile/ 已同步

---

## 一、整体架构

| 层 | 技术 | 说明 |
|---|---|---|
| 前端 | Electron + Vue3 + Naive UI（`gui/`） | 深色主题，左侧登录/设置，右侧搜索/浏览/下载 |
| 后端 | Python `gui_bridge.py` | 通过 stdin/stdout **NDJSON 协议**与前端通信，复用 `src/` 原有爬取下载模块 |
| 启动器 | `启动.bat` / `启动.vbs` | 自动查找可用 Python（过滤 WindowsApps stub），无黑框启动 |

**命令流向**：前端 `window.api.sendCommand` → main.cjs 写 stdin → gui_bridge.py 解析 JSON → 处理 → `emit()` 写 stdout → preload 转发 → App.vue `handlePythonEvent` 分发。

### 小小下载器单文件版（EXE 分享，自解压）
- **改名**：产品名“小小下载器”（原 BunkrDownloader），窗口标题/左侧标题/页面标题一致。
- **打包**：`gui/` 下设置镜像环境变量后 `npm run dist`（electron-builder portable，产物 `gui/release/小小下载器 1.0.0.exe`，~100MB 单文件，内嵌 `dist/` 前端 + `bunkr_bridge/` 打包后端，对方**无需 Python、无需 Node**）：
  ```
  $env:ELECTRON_CACHE='F:\BunkrDownloader-main\gui\.cache\electron'; $env:ELECTRON_BUILDER_CACHE='F:\BunkrDownloader-main\gui\.cache\electron-builder'; $env:ELECTRON_MIRROR='https://npmmirror.com/mirrors/electron/'; $env:ELECTRON_BUILDER_BINARIES_MIRROR='https://npmmirror.com/mirrors/electron-builder-binaries/'; npm run dist
  ```
- **分发**：根目录 `小小下载器/小小下载器.exe`（单文件，干净无个人数据）——直接压缩分享。
- **首次双击（自解压引导，main.cjs）**：单文件解压到临时目录运行 → 检测 `PORTABLE_EXECUTABLE_DIR` → 用 **robocopy**（系统自带）把完整程序释放到单文件旁边的“小小下载器”文件夹 → 启动文件夹内的完整程序 → 后台 PowerShell 延迟移除原单文件（引导器进程退出后文件才解锁，重试 30 次）。exe 本身在名为“小小下载器”的文件夹里运行时不嵌套（原地释放覆盖）。
- **数据目录**：程序文件夹内自动创建 `data/` **包裹全部运行数据**（不写 AppData）：
  - `data/` = Chromium userData（含 EX webview 会话）**同时是后端 cwd** → `cache/`（相册/缩略图/Cookie/设置 settings.json/tags 等全部长期数据）、`logs/`（前后端调试日志同一目录）、`downloads/` 都在里面；
  - **theme_cache.dat 隐藏**：main.cjs 注入 `XXD_THEME_CACHE_PATH` 环境变量，后端把加密账号库写到 `data/Cache/Cache_Data/theme_cache.dat`——Chromium 真实磁盘缓存目录内，与 data_0/data_1/f_000001 等缓存文件混在一起伪装；gui_bridge 启动时自动迁移旧位置的同名文件；
  - 文件夹程序靠 exe 旁 `xxd.app` 标记（内容=版本号）识别；**分享新版单文件**给已安装用户 → 版本不同自动重新释放程序文件（`data/` 数据完整保留）。
- **后端查找顺序**：EXE 旁 `bunkr_bridge/bunkr_bridge.exe` → 程序目录 `resources/bunkr_bridge/` → 系统 Python。
- **易错（重要）**：
  - **自解压必须用 robocopy，不能用 fs.cpSync**：Electron 的 fs 带 asar 补丁，cpSync 把 `app.asar` 当虚拟目录 → ENOTDIR 中断复制（已实测踩坑）；robocopy 是外部进程不受影响。
  - 单文件删除须**延迟重试**：引导器（SFX）进程存活期间其 exe 文件被锁；PowerShell `-EncodedCommand`（base64 UTF-16LE）传中文路径安全。
  - 首次启动稍慢（解压 100MB + 复制 400MB，属正常）；未配置自定义图标（用默认 Electron 图标）。
  - 分享新版前记得改 `gui/package.json` 的 version（marker 按版本判断是否重新释放）。

---

## 二、八站点支持

### 1. Bunkr（bunkr.xxx）
- URL：`/a/` 相册、`/f/` 文件、`/i/` 图片、`/v/` 视频。
- 后端复用 `src/` 模块（bunkr_api 等）。
- 相册缓存：`cache/albums/`（24h TTL，命中直接返回不重新解析）。

### 2. Coomer（xxxcoomer.com）
- 作者页 `/creator/{service}/{user_id}/{username}`，帖子页 `/post/{post_id}/{user_id}/{service}/{username}`。
- 代码全部集中在 `gui_bridge.py`（src/ 无改动）。
- 媒体提取：帖子页 `.post-body` 内 `img`/`source` 标签，排除 `/istorage/`（作者头像）。
- **易错**：视频直链带签名参数会过期，下载时必须重新抓帖子页取新链接；图片直链永久有效可回退。
- **易错**：站点限流很凶，连续请求会 IP 级封锁 → `_throttle_search()` 限流 + 指数退避重试。

### 3. Pawchive（pawchive.pw，Kemono 架构）
- 画师页 `/{service}/user/{user_id}`，帖子页 `/{service}/user/{user_id}/post/{post_id}`。
- API：`/api/v1/{service}/user/{id}/posts?o={offset}`（每页 50）等。
- 登录：POST `/account/login`，会话 cookie 存 `cache/pawchive_session.json`，启动时恢复并 emit silent 登录事件。
- 帖子详情页：画师链接可点、标签可点击搜索、附件预览、解析下载按钮；工具栏"我的收藏"拉取服务器收藏画师。
- **画师子项目视图**：点开搜索结果中的画师卡片 → `pawchive_artist_posts` 命令拉取该画师**全部帖子**（分页 API + 并发补全附件详情，Semaphore(4)），按发布日期倒序展示为子项目列表（缩略图、标题、日期、文件数、视频/压缩包徽章、内容摘要）；**鼠标悬浮显示完整详情**（标题/日期/文件数/文件名列表/内容）；点子项目进帖子详情；工具栏"批量解析全部"直接解析画师全部媒体；缓存 `cache/pawchive_posts/{service}_{user_id}.json`（TTL 内命中直接返回，标"缓存数据"）。
- **下载目录归类**：自动按 `画师名/YYYY-MM/帖子标题` 存放（`pawchive_subfolder` 设置：date_post/date/post/none，支持 `pawchive_folder_template` 自定义模板）。
- **易错**：下载限速 → `_pawchive_throttle_download()` 强制两次下载间隔 ≥1s；必须单连接下载（`connections=1`）。

### 4. ExHentai（exhentai.org）
- 登录需持久化 cookie（igneous、ipb_member_id、ipb_pass_hash 等），存 `cache/exhentai_cookies.json`；igneous 频繁刷新，`_exhentai_fetch` 自动从 Set-Cookie 更新。
- 界面：RightPanel 内嵌 `<webview>`（独立会话 `persist:exhentai`），工具栏后退/前进/刷新/主页/地址栏，复刻原站搜索页布局（分类/评分/种子/页数过滤，按钮汉化）。
- cookie 同步：webview 登录后点"同步Cookie"→ IPC → 存给 Python 后端并验证。
- 画廊详情页：标题、发布者、元数据、分组标签（可点击搜索）、父画廊跳转；"我的收藏"复用搜索结果视图，分页与原站一致。
- **点开画廊自动解析展示**：点击搜索结果中的画廊卡片 → App.vue `handleExOpenGallery` 并发 `exhentai_gallery_info`（元数据先到，detail 视图短暂可见）+ `inspect`（解析全部图片直链）→ `inspect_complete` 后 fileList.length>0 触发共用文件列表视图（自带全选/反选/仅选可下载/批量下载）；detail 视图条件加 `fileList.length === 0` 自动隐藏但 `exGalleryDetail` 状态保留；文件列表工具栏新增 `ℹ️ 画廊信息` 切换按钮 + `.ex-inline-info` 内联面板（发布者/时间/语言/大小/页数/评分/收藏/分组标签可点击搜索，默认展开）；点"后退"回到搜索结果（同步清理 exGalleryDetail/paPostDetail/paArtistPosts/iwDetail/haDetail/orDetail）。
- 磁力流程：`exhentai_get_torrents`（种子页）→ 弹窗选种 → `exhentai_get_magnet`（下载 .torrent 解析 infohash）→ 复制/系统下载器打开；.torrent 可保存到 `downloads/torrents/`。
- **隐藏标签（长期保存）**：搜索选项栏"🚫 隐藏标签"醒目按钮 → 展开管理面板手动输入标签名；存 `cache/exhentai_hidden_tags.json`；匹配规则不区分大小写：填 `xxx` 隐藏所有命名空间下的 `xxx`，填 `female:xxx` 只隐藏 female 命名空间下的；搜索结果和我的收藏都经过 `_exhentai_filter_hidden_tags` 过滤，隐藏数量随 `hidden_count` 字段返回；每个标签 chip 右侧"×"删除。命令：`exhentai_get_hidden_tags` / `exhentai_add_hidden_tag` / `exhentai_delete_hidden_tag`；启动时自动拉取列表。
- **易错**：请求节流 `_exhentai_throttle()` 间隔 ≥1.2s 防 509；无 cookie 返回 sadpanda；访问需代理；缩略图缓存需带账号 Cookie + 代理。

### 5. X / Twitter
- **登录**：Cookie 方式（`auth_token` + `ct0` 两个独立输入框分别填写，兼容粘贴带名称前缀或整段 Cookie），存 `cache/twitter_cookies.json`；`verify_credentials` API 验证；支持一键抓取浏览器 Cookie。
- **搜索（内容搜索 + 博主直达）**：`@用户名`（仅限博主）→ UserByScreenName 验证后**直接解析 TA 的全部媒体**；任意关键词 → **SearchTimeline GraphQL 内容搜索**（`filter:media` 只搜带媒体推文，product=Top），返回推文卡片（作者+摘要、首图、媒体数），点击解析下载；queryId 空注册，首次 404 自动刷新。
- **易错（内容搜索必须 POST）**：SearchTimeline 的 GET 请求会被 `x-client-transaction-id` 头校验拦截 404 → `_twitter_api_get(..., use_post=True)` 走 POST（payload 为 `{queryId, variables, features}`），且使用专用 `_TW_SEARCH_FEATURES`（与 UserByScreenName/UserMedia 的 features 集不同）。
- **解析速度与反惩罚**：GraphQL 请求统一 `_twitter_throttle()` 节流（间隔 ≥0.8s，顺序请求仍远低于浏览器并发量，规避限流惩罚）；qid 自动刷新 + 404 重试保证长期能用。
- **解析**：用户主页 → UserMedia 时间线自动翻页（游标去重，TimelineTimelineModule entry + TweetWithVisibilityResults 解包）；单条推文 → TweetDetail GraphQL + syndication API 回退（免登录）。
- **点开链接自动解析展示**：浏览模式（twFollowMode==='browse'）下点推文卡片 → 自动 emit `open-album` → handleInspect → fileList 接管共用文件列表视图（带批量下载），"X 点开链接自动解析展示"天然满足；搜索模式点用户卡进入 user 视图保留交互按钮，"解析 TA 的全部媒体"为显式按钮（避免自动解析抢占用户视图交互入口，可点"后退"在两视图间切换）。
- **下载**：媒体直链（pbs.twimg.com / video.twimg.com）永久有效，走代理流式下载；目录 `用户名/YYYY-MM/推文内容`（`twitter_subfolder` 设置：date_post/date/post/none）；图片 URL 格式 `media_url_https?name=orig`。
- **代理**：默认 `http://127.0.0.1:10809`，国内必须；`twitter_set_proxy` 命令。
- **queryId 轮换**：X 每隔数周轮换 GraphQL queryId，`_twitter_api_get` 遇 404 自动从 x.com 首页 main.js 抓新 queryId 并重试，缓存 `cache/twitter_qids.json`。GraphQL features 按查询拆分（UserByScreenName 11 项 / UserMedia 21 项，源自 x-spider）。
- **关注列表/关注管理**：
  - 列表用 **v1.1 稳定端点**（无 queryId 轮换问题）：`friends/list.json`（关注列表）、`followers/list.json`（关注我的人），Bearer + Cookie + ct0 三件套，每页 200 人 cursor 翻页；支持 `screen_name` 参数查看**任意用户**的关注/粉丝列表。
  - 关注/取关：v1.1 `friendships/create.json` / `friendships/destroy.json` 为主，失败回退 GraphQL CreateFollower / DeleteFollower mutation（queryId 同样支持自动刷新）。
  - 前端：X 站工具栏四个按钮（浏览模式 / 关注列表 / 关注我的人 / 我的分类）→ 用户卡片（头像、昵称、@handle、简介、关注/粉丝/推文数）→ 卡片按钮：**媒体**（解析全部媒体）、**分类**、**关注/取消关注**（带确认弹窗）；点卡片进入**用户主页**。
- **用户主页（点开关注的人）**：显示 TA 的资料卡 + 操作行：[解析 TA 的全部媒体] [TA 的关注] [关注 TA 的人] [分类] [关注/取关]。从 TA 的列表可继续点更深用户，"← 返回"按导航栈逐层回退（App.vue `twNavStack`，最多 20 层）。
- **浏览模式（最近博主更新）**：
  - 后端 `twitter_browse(offset)`：取关注列表（有推文的博主优先），逐个拉 UserMedia 最新一页（复用现有 GraphQL 代码），每人最多取 2 条带媒体推文，按发布时间倒序合并，带进度事件（done/total）。
  - **分批加载**：每批 30 人（`offset` 偏移量），信息流底部"↓ 点击继续更新（下一批博主）"按钮加载下一批；后端追加模式与旧缓存按推文 id 去重合并后统一排序（缓存上限 600 条），全部加载完显示"已加载全部"。
  - 刷新：浏览模式工具栏"刷新"按钮重新从第一批拉取。
  - 前端信息流卡片：博主头像/昵称/时间（点头像行进 TA 的主页）、推文文本（最多 4 行）、媒体缩略图网格（最多 4 张，视频带 ▶ 角标）；点卡片解析该推文媒体。
  - 每次请求节流 1.2s，30 人约 36 秒，刷新时有进度条提示。
- **X 本地搜索（搜缓存内容）**：
  - 工具栏搜索框（X 站四个按钮右侧）实时过滤**已缓存内容**：浏览模式信息流（推文文本、#tag、博主昵称/@handle、推文链接）+ 关注列表/我的分类（昵称、@handle、简介、分类 tag）。
  - 搜索结果视图分"用户卡片 + 推文卡片"两类展示；"← 返回"或清空关键词恢复原视图（快照 `twSearchSnapshot`）。
  - 纯前端过滤，不发网络请求；数据未加载时提示先打开"关注列表/浏览模式"载入数据。
- **缓存机制（重新点击调入后更新）**：
  - 关注列表（含任意用户的）：`cache/twitter_user_lists/{mode}_{screen_name|self}.json`，点击先显示缓存再后台拉取最新；刷新失败且已有缓存时静默保留缓存（仅记日志）。
  - 浏览模式：`cache/twitter_browse_cache.json`，点击先显示缓存再后台刷新。
  - 清除推特缓存（左侧登录下方大按钮，带确认弹窗）：删除 qid 缓存 / 关注列表缓存 / 浏览模式缓存 / 用户媒体解析缓存（`cache/albums/twitter_*.json`）；**保留登录 Cookie 与关注分类数据**。
- **关注分类（母子 tag）**：
  - 母子类 tag 文件夹：`cache/twitter_follow_tags.json`（`[{name: 母类, children: [子类]}]`）。
  - 已归类关注：`cache/twitter_follows.json`（`{user_id: {screen_name, name, avatar, url, parent, child, saved_at}}`）。
  - 左侧（X 登录卡片下方）："关注分类管理"面板：添加母类/子类、删除母类（连带子类）/子类。
  - 关注列表卡片点"分类" → 弹窗：下拉选已有母类/子类或直接输入新名（保存时自动创建）；母类留空保存 = 移除归类；已归类卡片显示 tag 徽章（"改分类"）。
  - "我的分类"视图：按 母类/子类/用户名 排序浏览全部已归类关注，可点行解析媒体。
  - 归类时填写的分类若不存在会**自动创建**，方便快速录入。

### 主题（日间 / 夜间）
- 左上角"日间/夜间"按钮切换（太阳/月亮图标），设置 `theme: 'dark'|'light'` 持久化。
- naive-ui 主题动态切换（light=null）；`html.light-mode` 根类 + App.vue 全局 CSS 覆盖主要容器/卡片/文字颜色。
- **已适配日间模式的区域**：左右面板、URL 栏、账号卡片、搜索/详情/磁链卡片、X 工具栏/用户卡片/推文卡片/关注视图、**左侧圆形导航按钮**（下载状态/日常标签/本地收藏/识图/翻译）、**底部下载进度/日志/历史三个标签页**、设置区、分类管理面板、EX 浏览器工具栏、Hanime1/Oreno3D 主页与详情视图。
- 圆形按钮配色：夜间 `#2a2a30` 底 + `#63e2b7` 高亮；日间白底 + `#18a058` 高亮。

### 6. Iwara（iwara.tv，MMD 视频站）
- **登录（密码长期保存 + 自动续期）**：邮箱 + 密码（左侧表单）→ `POST /user/login` 换 Bearer token；**密码与 token 一起加密保存**（theme_cache.dat），token 过期（JWT exp 到期或服务器 401）时 `_iwara_auto_relogin()` 自动用保存的密码重新登录，长期免输密码；账号档案也保存密码，切换档案后同样自动续期。media token（TTL ~1h）自动用 user token 换新（`_iwara_media_token`）。命令：`iwara_login` / `iwara_logout` / `iwara_check_login` / `iwara_set_proxy`。
- **搜索**：关键词 → 官方搜索端点 `GET /search?query=&type=videos&page=&limit=32`（`/videos` 的 query 参数会被服务端忽略——这是早期"搜索成功但结果不对"的根因）；`@用户名` → 先 `/profile/{username}` 取 user_id 再拉 TA 的视频；分页走顶底页码条（前端页码 1 起始，API 0 起始，`iwara_search` 内换算）；卡片含作者/播放/点赞/日期。
  - **易错（/search 参数）**：`type` 必须是复数 `videos`（官网搜索页同款参数），单数 `video` 或附加 `sort`/`rating` 参数会触发服务端 500；`/search` 走 `_iwara_api_get`（自带 X-Site/Origin/登录态/代理/节流），IW站/AI站共用。
- **解析**：视频页 `/video/{id}` 或用户主页 `/profile/{username}`（翻页拉全部视频 ID，上限 20 页）；每个视频取 `/video/{id}` 详情（fileUrl + 缩略图）。
- **下载（默认最高画质 Source）**：`_iwara_resolve_best_url` 请求 fileUrl（带 `X-Version` 签名 = sha1(`路径末段_expires_盐`)，盐 `mSvL05GfEmeEmsEYfGCnVpEjYgTJraJN`），从源列表选 Source > 540 > 360，取 `src.download` 直链流式下载；**下载时重新解析**（fileUrl 的 expires 会过期）。
- **目录**：`作者名/YYYY-MM/视频标题.mp4`（`iwara_folder_template` 自定义模板：{date}/{date_full}/{title}/{id}）。
- **代理**：`iwara_proxy` 设置（默认空 = 直连，国内建议填），API / 下载 / 缩略图（files.iwara.tv）统一走 `_iwara_session`。
- **节流**：`_iwara_throttle()` 间隔 ≥0.5s 防 API 限流。
- **IW站/AI站切换**：`iwara_set_site` 命令切换 `X-Site` 请求头（`www.iwara.tv` / `www.iwara.ai`），同一 API（api.iwara.tv）区分内容；站点持久化 `cache/iwara_site.json`，登录信息两站共用；左侧已登录行与未登录表单、右侧工具栏均可切换，切换后自动刷新主页。
- **主页（最近更新）/ 我关注的更新（订阅流）**：进入 Iwara 站点自动 `GET /videos?sort=date&limit=32`（`iwara_home`，mode=''）；工具栏"我关注的更新"按钮 → `iwara_home(mode='subscribed')` 带 `subscribed=true` 参数只拉已关注作者的最新投稿（需登录），"加载更多"沿用当前模式；卡片与官网一致：缩略图 + 时长/R-18/Ecchi 角标 + 播放/爱心 + 标题 + 作者 + 相对时间（xx天前）。
- **我的关注 / 我的好友**：`GET /user/{我的id}/following` 与 `/user/{我的id}/friends`（每页 50，翻页追加）；用户卡片可"看内容"（@用户名搜索）与 关注/取消关注。
- **关注/取关**：`POST` / `DELETE /user/{id}/followers`（`iwara_follow`），成功后同步更新关注/好友列表与详情页状态。
- **视频详情页**：点搜索结果/主页卡片 → `iwara_video_detail`：`/video/{id}` 完整信息（fileUrl 解析出的最高画质播放直链，`//` 开头补 `https:`）+ 简介正文 + tags（**tag 的 id 即标签名**，如 musclegirl，可点击搜索）+ 首页评论（`/video/{id}/comments`），评论"加载更多"翻页（`iwara_video_comments`）；作者行可关注/取关/进主页；点评论者头像/昵称进主页；rating 字段：general / ecchi / r18。
- **易错**：未登录也能搜索/下载公开视频（media token 仅在有 user token 时附加）；私有视频需登录；扩展名按真实 MIME（.mp4/.webm）调整；iwara.ai 直连 api.iwara.ai 会 SSL 错误——必须走 api.iwara.tv + `X-Site: www.iwara.ai` 头。

### 7. Hanime1（hanime1.me，H 动漫在线站，X 站类型：账号体系 + 评论 + 收藏）
- **登录（邮箱 + 密码，会话长期保持）**：POST `/login`（需先 GET 取 CSRF `_token`）；会话 cookie（XSRF-TOKEN / remember_token 会刷新）与邮箱密码一起**加密存 theme_cache.dat**；会话失效时用保存的密码自动重登（`hanime_check_login` 触发）。命令：`hanime_login` / `hanime_logout` / `hanime_check_login` / `hanime_set_proxy`。账号档案存完整凭据（cookies+邮箱密码），切换档案后自动恢复并静默验证。
- **主页**：`hanime_home` 抓首页各分区（`div.home-rows-videos-wrapper`，分区标题取前面最近的 h3），每区最多 20 条卡片；顶部分类按钮（genre：裏番/泡麵番/Motion Anime/3DCG/2.5D/2D動畫/AI生成/MMD/Cosplay）+ 排序（最新上市/最新上傳/排行等）。
- **搜索**：`GET /search?query=&genre=&sort=&page=`（Laravel 分页，`rel="next"` 判断 has_more）；卡片含时长/观看数/好评比例/作者/时间。
- **视频详情页**：`hanime_video_detail` 解析 `/watch?v=`：标题/简介/上传者/观看数/日期/**tags（可点击搜索）**/收藏状态；播放源 `<source>` 多画质（720/480/1080），在线播放取最高画质经本地媒体代理；评论走 `GET /loadComment?id=&type=video`（**需登录**才返回内容），支持发表评论（`hanime_add_comment`，POST /createComment 带 CSRF）。
- **收藏/取消收藏**：`hanime_save_video`（POST /save，"稍後觀看"播放清单）；用户中心 tabs：觀看紀錄/稍後觀看/讚好的影片/上傳的影片/審核中的影片（`hanime_user_videos`，`/user/{id}/{histories|saves|likes|uploaded|uploading}` 分页）。
- **下载**：`hanime_batch_download`（主页/搜索/用户中心勾选批量）或解析 `/watch?v=` URL；MP4 直链带 `secure` 签名会过期 → **下载时重新解析最高画质**；目录 `上传者名/YYYY-MM/标题.mp4`。
- **代理**：`hanime_proxy` 设置（默认 `http://127.0.0.1:10809`，国内必须）；请求节流 ≥0.6s（连续请求过快被 Cloudflare 拦截）；媒体域名 vdownload.hembed.com 走代理 + Referer。
- **易错**：登录表单必须带 `submit=login` 字段；评论/收藏操作前先查页面登录态，失效自动重登一次；页面是服务端渲染（BeautifulSoup 解析，无 API）。

### 8. Oreno3D（oreno3d.com，MMD 视频聚合索引站，纯资源站类型：无需登录）
- **纯索引站**：视频本体全部在 iwara.tv，站内只存缩略图和元数据；**无需登录**，裸请求可访问全部内容。
- **无账号体系（2026-08-28 调研确认）**：站点无 /login /register 页面、无用户表、无会话 cookie；`toggleFavorite(id, modelName)` 函数（定义于 oreno3d.com/js/main.js）纯前端实现，收藏列表存浏览器 `localStorage["favorite-{model}-list"]`；axios 仅用于 `/favorite-{model}-list/{id}` 列表查询和 `/api/get-movies-by-id-order-by-column`，均不要求登录。本工具沿用本地 JSON 收藏（`cache/oreno_favorites.json`）是正确做法，无需也不应实现登录/账号切换。
- **列表/主页**：`GET /`（默认人気排序）`?sort=hot|favorites|latest|popularity&page=N`（Laravel 分页）；卡片 `a.box.pop_separate`（标题/缩略图/作者/tags/播放数/点赞数）。
- **搜索**：`GET /search?keyword=`（可与 sort 组合）；**标签页** `/tags/{id}`、**作者页** `/authors/{id}`、**标签索引** `/tags`（弹窗选择）。
- **详情页**：`oreno_detail` 解析 `/movies/{id}`：标题/大图/作者（可点进作者页）/tags（可点）/日期/统计 + 从 `a.video-watch-btn2` 提取 **iwara 视频 ID** → 调 Iwara API 解析最高画质播放直链（在线播放/下载复用 Iwara 全部逻辑）。
- **下载**：`oreno_batch_download`（勾选批量）：逐个解析详情页取 iwara ID → `_iwara_build_items` 复用 Iwara 下载（最高画质，下载时重新解析）；也可直接解析 `oreno3d.com/movies/{id}` URL。
- **代理**：`oreno_proxy` 设置（默认空 = 直连）；节流 ≥0.5s；页面含大量广告脚本（waqool/lmadps 等），解析只取结构化标签不受影响。

### 站点按钮与导航布局（2026-08-28 改版）
- **站点切换按钮竖排三类**：二次元（pawchive / ex / iwara / H站 / O3D）、三次元（coomer）、综合类（bunkr / x）。
- **左侧圆形导航按钮**：下载状态 / 日常标签 / 本地收藏 / **识图（占位，未实现）** / **翻译（占位，未实现，规划用谷歌/Edge 插件翻译网页内容）**；"我的收藏"已移除，功能在 PA 站工具栏。
- 登录信息下方四个按钮（保存当前 / 删除 / 重新登录 / 退出登录）**横向单行排列**（`justify-content: space-between`），节省纵向空间；账号切换下拉独占一行。
- **未登录也显示已保存账号档案**：登录表单下方常驻"切换已保存账号"下拉 + 删除档案按钮（有档案时才显示），当前账号过期可直接换另一个，无需先重新登录。
- **站点快捷工具行（所有站点可见，登录区下方）**：①"打开网站"按钮 + 浏览器下拉（默认/Chrome/Edge/Firefox）→ IPC `open-with-browser`（`cmd /c start <prog> <url>` 走 App Paths 定位浏览器，默认=shell.openExternal）；②"帮助"按钮 → 弹窗展示该站点使用技巧（`SITE_HELP_TIPS`，含 Bunkr 找资源方法/各站登录与下载要点），内容可选中复制 + 一键复制全部。
- 左侧圆形导航按钮组**横向排列**（`flex-direction: row`，自动换行），间距 12px。

---

## 三、登录与账号系统

- **加密账号存储（全站点统一）**：所有站点的登录凭据与账号档案集中存在根目录伪装文件 `theme_cache.dat`（PBKDF2 + 密钥流 + HMAC 加密；解密密码 `yueyangxi` 或 `yueyangxi1995` 均可）。文件缺失/损坏 → 自动创建空白文件（删除即清空全部账号）；首次访问自动迁移并删除旧明文文件（cache/accounts.json、各站 cookie/token json、pawchive_session.json）。`清除缓存`只删 cache/，不影响账号；分享文件夹前删掉该文件即可清空登录。
- Iwara 登录长期保持（user_token + **密码**均加密存储，token 过期自动用密码续期）；账号卡片支持保存当前/删除/重新登录/退出登录，多账号切换（档案含密码，切换后同样自动续期）。
- **Iwara 登录误判修复（2026-08-27）**：`GET /user` 新版 API 返回 `{"balance":.., "user":{...}}`（用户对象嵌在 user 字段里），旧代码直接读顶层 `id` 读不到 → 误判"登录失效"要求重新填密码。修复：`_iwara_me_user()` 统一解包（新版取 `me["user"]`，旧版直接返回），登录/登录检查/自动续期三处全部改用。
- 切换站点时登录状态不丢失；账号卡片常驻左侧；支持登出、切换账号（账号档案 `save_account`/`switch_account`/`delete_account`）。
- 登录失效判断：对比缓存信息区分"网络问题"与"账号异常"（`network_issue` 标记），提示傻瓜式解决方法。
- 未登录时显示登录提示 + "打开登录页（浏览器）" + **一键抓取浏览器 Cookie**（自动登录）。
- 点击内容自动复制到剪贴板。

### Iwara 批量解析下载
- 工具栏"批量解析下载"按钮（我的关注/我的好友右侧）：点击后当前列表进入勾选模式（卡片带 ✓ 选中框、绿框高亮），点"开始下载(n)"提交。
- 我的关注/我的好友：勾选用户 → 每个用户单独一个下载任务（自动解析其全部视频，目录=作者名/...，与单用户解析下载一致）。
- 主页最近更新：勾选视频 → 全部合并为一个任务下载。
- 后端 `iwara_batch_download`：逐用户拉视频列表 → 逐视频取详情 → `download_manager.submit`；进度经 `iwara_batch_progress`（工具栏橙字）与 `iwara_batch_done`（完成通知，含失败明细）回传。

## 四、下载系统

- 下载任务管理器（独立窗口 + 悬浮窗波浪进度条），支持暂停/恢复/取消/删除、下载完关机。
- 通用设置：路径、并发连接数、并发文件数、限速、重试、保留原始文件名、按类型分类、日期戳等；每站点自定义子文件夹模板（`{date}/{title}/{id}` 变量）。
- **文件列表显示模式切换**：列表操作栏右侧"方格/列表"按钮 → 小方格模式（缩略图网格、点击卡片切换选中、绿色边框+✓ 高亮、新文件"新"角标、错误项半透明）或横向详细表格（默认）；选择持久化（localStorage `file_view_mode`），全选/反选等操作两种模式通用。
- **下载去重（本次新增）**：
  - 开关"跳过重复项目"（`skip_duplicates`）：下载前检查本地同路径同名文件——**大小一致 = 已下载过，直接跳过**（日志"跳过重复"）；大小不同 = 重名冲突，按改名规则处理。
  - 开关"重名手动改名"（`manual_rename`，仅在上开关开启时显示）：**开 = 弹窗手动输入新文件名**（显示原文件名/已存在大小/本次大小，预填建议名，确认后以新名重新下载）；**关 = 自动序号顺延**（`file.jpg → file (1).jpg → file (2).jpg`）。鼠标悬停开关有完整解释 tooltip。
  - 实现位置：`gui_bridge.py` 的 `_resolve_duplicate()` 统一处理三条下载路径（通用 download_one / Bunkr 列表页下载 / Twitter 直链下载）；Twitter 下载前 HEAD 预取大小用于对比。
  - 改名弹窗回传：前端以 `rename_map`（键=原文件名或 item_page）重新发送单文件下载命令，`_apply_rename_map()` 应用。多个冲突进队列逐个弹窗。

## 五、在线播放 / 预览（全站点，2026-08-27 新增）

- **入口**：文件列表（方格/列表两种模式）每个条目右上角"播放/预览"按钮（视频▶ / 图片🖼 图标），点开弹窗。
- **前端**（RightPanel.vue 预览弹窗）：视频用 `<video controls autoplay>` 可拖进度条；图片大图预览点图切换下一个；左右方向键翻页；底部显示文件名/大小与"上一个/下一个"导航。
- **本地媒体代理**（gui_bridge.py `start_media_proxy`）：后端启动时在 `127.0.0.1` 随机端口起 aiohttp HTTP 服务，`GET /media?url=<远端直链>` 流式转发（256KB 分块），支持 **Range 透传**（视频拖进度条必需）。前端所有 `<img>/<video>` 一律走代理地址 `http://127.0.0.1:{port}/media?url=...`，不直连远端（国内直连 twimg/iwara/EX 不通）。
- **按站点路由**（`_media_route`）：twimg → X 代理；iwara → Iwara 代理 + Bearer token（私密视频）；exhentai/e-hentai/ehgt/hath.network → 账号 cookie + EX 代理；pawchive → 会话 cookie；hanime1.me/vdownload.hembed.com → Hanime1 代理 + Referer；oreno3d.com → Oreno3D 代理。域名白名单 `_MEDIA_ALLOWED_KEYWORDS` 防代理被滥用。
- **懒解析直链**（`resolve_media_url` 命令）：Coomer/Pawchive/X/Iwara 条目本身带 `media_url` 直接返回；**Bunkr** 条目页走签名 API 现解析直链；**ExHentai** 图片页解析 `#img` 直链。成功 emit `media_url_resolved`，前端回填到条目并经代理播放；失败给出提示（部分内容需下载后观看）。
- **事件流**：`media_proxy_ready`（端口通知，App.vue 存 `mediaProxyPort`）→ 点预览时若条目无 `media_url` 则发 `resolve_media_url` → `media_url_resolved` 回填（预览弹窗监听同一对象自动刷新）。

## 六、搜索 / 历史 / 收藏

- 搜索：balbums.st（Bunkr 相册索引站，含反爬应对）；Coomer/Pawchive/EX 各自搜索；X 用户搜索 + 内容搜索。EX 搜索复刻原站过滤（分类勾选、最低评分、仅种子、页数范围），翻页与原站一致。
- 搜索历史：`cache/search_history.json`（最多 100 条，query+site+mode 去重置顶），左侧"日常标签"面板点击快速搜索。
- 本地收藏：`cache/local_favorites.json`，搜索结果卡片 ♥ 收藏，左侧爱心面板点击直接切站点并解析 URL。

### 缓存优化（防"缓存过多不显示"）
- **缩略图缓存 LRU 清理**：`cache/thumbnails/` 体积上限 500MB，超过后按文件修改时间删最旧的，清到 400MB 为止；每小时最多执行一次（`_cleanup_thumbnail_cache()`，每次新缩略图落盘后触发）。
- **缩略图可靠性（防加载失败）**：后台缓存并发限制 6（Semaphore，全量并发反而触发限流批量失败）、失败自动重试 1 次、超时 20s；缓存完成后 emit `thumbnails_cached`（url → thumb://local 映射），前端 `applyCachedThumbnails()` 把当前视图里还在直连原图的条目（twimg/iwara 等国内直连不通的域名）立即换成本地缓存路径 → 首次查看不再一直转圈失败。
- **数据缓存 TTL**：相册缓存 `cache/albums/` 24h、PA 画师帖子列表 `cache/pawchive_posts/` 按 TTL 过期、X 用户列表/浏览缓存点击时后台刷新（先显缓存再更新，失败静默保留旧缓存）。
- 各站点"清除缓存"按钮（X 站在左侧登录下方，其余站点在设置区）用于手动兜底。

### 下载性能（防"任务多卡顿"）
- **进度事件节流**：`GuiLiveManager.update_task` 每个文件 ≥0.3s 才 emit 一条 `file_progress`（完成 100% 必发）；此前每个 chunk 一条事件，多文件并发时 IPC 洪泛导致界面卡死。
- **任务快照防抖**：`emit_snapshot()` 默认 0.5s 合并推送（`tasks_snapshot` 含全部任务+全部文件，任务多时单条巨大；结构性变化如提交/暂停/取消/完成用 `immediate=True` 立即推）。
- **落盘防抖**：`_save()` 默认 1s 合并写盘（此前每个文件状态变化全量重写 tasks JSON，千级文件时磁盘 IO 拖垮后端）。
- **快照深拷贝**：`snapshot()` 经 `_json_safe_task()` 深拷贝，避免序列化时并发修改任务对象导致崩溃。

## 七、Cookie 一键抓取（fetch_cookies.py + 抓取Cookie.bat）

- 根目录双击运行，扫描内嵌浏览器/Chrome/Edge/Brave/X-Spider(WebView2)/Firefox 的已登录 Cookie，按 SITES 列表分类输出 cookies.txt。
- **新增网站只需在 SITES 列表加一行**：`{"name": 显示名, "domains": [...], "important": [关键Cookie名], "usage": 用途}`。
- Chrome v20 (App-Bound) 解密链：Elevator COM → 失败走管理员路径：SYSTEM DPAPI → 用户 DPAPI → flag 1/2 内置 AES/ChaCha 密钥 → flag 3 CNG KSP 密钥。

## 八、update 更新包机制（重要）

- **所有代码改动必须同步复制到 `update/` 文件夹**，保持相同目录结构。
- 用户把 `update/` 分享给他人，覆盖到根目录即完成更新。
- 之前更新过的文件再次改动时，直接覆盖 update 中的对应文件。
- 需同步：根目录脚本、`gui/` 源码（不含 node_modules）、改动的 `src/` 模块、**前端构建产物 `gui/dist/`**（改前端后重新 build 再复制）。
- **EXE 发布后更新规则（必须遵守）**：已发布"小小下载器"单文件 EXE，之后**所有更新必须同时产出：① 新的单文件 EXE（gui/ 下带镜像环境变量 `npm run dist`，产物复制到根目录 `小小下载器/`）；② 包含本次全部改动内容的 exe 更新包（update/ 完整同步）**，两者缺一不可。分享新版前改 `gui/package.json` 的 version（marker 按版本判断是否重新释放程序文件，data/ 数据保留）。改 gui_bridge.py 后先运行 `打包后端.bat` 重打包 `bunkr_bridge/`，再构建 EXE。

## 九、已知坑（必读）

| 坑 | 原因/解决 |
|---|---|
| 中文命令乱码 | Windows 管道 stdin 默认 GBK 解码，Electron 发 UTF-8。修复：main.cjs 设 `PYTHONIOENCODING=utf-8` + gui_bridge.py 启动时 `sys.stdin/stdout.reconfigure(encoding='utf-8', errors='replace')` |
| Vue Proxy 无法 IPC 克隆 | 发送前必须 `JSON.parse(JSON.stringify(obj))` |
| WindowsApps Python stub | findPythonPath 过滤 WindowsApps 路径，逐个 `--version` 验证，失败通知链 `backend_error` → 前端红色错误框（含傻瓜式解决方法） |
| X GraphQL queryId 轮换 | 404 时自动从 x.com 首页 main.js 抓新 qid 重试；v1.1 端点（关注列表）不受影响 |
| Coomer/Pawchive 限流 | 强制节流（≥1s/1.2s）+ 退避重试 + 单连接 |
| EX sadpanda | 无 cookie/代理时的表现；cookie 持久化 + webview 同步解决 |
| 重名视频直链过期 | Coomer 视频下载时重新抓帖子页取新签名链接 |
| debug 日志 | 专门日志文件夹，按时间记录 |

## 十、改进思路（待做）

- X 关注列表支持 GraphQL 备用（当前 v1.1 若被下线需要兜底）。
- 关注分类支持拖拽排序、导出/导入（json 分享）。
- 下载去重可扩展"全局指纹去重"（跨路径按 hash）。
- 关注列表批量取关/批量分类（勾选模式）。
- Coomer/Pawchive 帖子详情页统一化（复用 EX 的详情组件）。
- 前端代码分割（当前 bundle 1.5MB，可用动态 import 优化首屏）。

## 十·五、本轮新增功能（2026-08-28）

### 1. 搜索框站点化辅助提示
- 各站点搜索框 placeholder 已按 site 切换文案：Coomer/Pawchive（画师/标签）/ExHentai/X（@用户名）/Iwara/Hanime1/Oreno3D/EroMMDTube/ASMR/Bunkr。
- 见 RightPanel.vue `inputPlaceholder` computed（line 4259 起），切换站点自动更新提示。

### 2. 在线预览改进：默认静音 + 音量持久化
- 修改位置：RightPanel.vue 预览弹窗（line 2941 起）。
- **默认静音**：video 标签 `:muted="previewMuted"`（首次访问 `preview_muted !== '0'` 即默认 true，符合浏览器 autoplay 策略）。
- **音量持久化**：`localStorage.preview_volume`（0~1，默认 1）+ `localStorage.preview_muted`。
- **行为**：
  - `@loadedmetadata` → 应用保存的 muted/volume（首次或换源时）。
  - `@volumechange` → 用户用浏览器原生 controls 调音量后同步回 ref + 持久化。
  - 自定义 UI：🔇/🔊 静音按钮 + 音量滑块 + 百分比文字，仅视频时显示。
  - 拖动滑块音量 >0 时自动取消静音。
  - 适配日间模式（`html.light-mode .preview-volume` 浅色滑轨）。

### 3. 翻译按钮（有道智云 API）
- 占位"开发中"按钮接入有道智云文本翻译 API（https://openapi.youdao.com/api）。
- **后端**（gui_bridge.py）：
  - `translate_youdao(text, from_lang='auto', to_lang='zh')` 函数 + 命令分发 `translate_youdao`。
  - 签名：`sha256(app_id + input_str + salt + curtime + app_secret)`，signType=v3；input_str = 文本 ≤10 全量 / >10 取首3+长度+末3。
  - 4 域名容灾无须（单一 openapi.youdao.com），失败/未配置/错误码统一回 `ok=False` + error。
  - DEFAULT_SETTINGS 新增 `youdao_app_id` / `youdao_app_secret`（默认空）。
- **前端**（LeftPanel.vue）：
  - 翻译占位面板 → 完整面板：源语言/目标语言下拉（9 种）、输入文本框（Enter 翻译）、📋 粘贴/✕ 清空/翻译 按钮、结果区（成功显示翻译+复制按钮，失败显示错误）。
  - 折叠的"⚙ 有道 API 配置"区：应用 ID + 密钥输入（密钥默认隐藏可切换显示），保存即用无需重启。
  - 用户在 https://ai.youdao.com/ 注册并创建"文本翻译"应用获取 ID/密钥（新用户有免费额度）。
  - App.vue `handleTranslateYoudao` → `sendCommand({cmd:'translate_youdao', text, from, to})`；监听 `translate_result` 事件回填到 `translateResult` ref 传给 LeftPanel。

### 4. 设置-更新模块（GitHub 仓库检查）
- 在设置区底部新增"检查更新（GitHub）"区块。
- **后端**（gui_bridge.py）：
  - `check_github_update()` 函数 + 命令分发 `check_github_update`：调 `api.github.com/repos/secondashes/xiaoxiaodownloader/commits/main`，取最新 commit（sha/message/date/author/html_url）+ 顺便拉 `releases/latest`（无 release 静默跳过）。
  - 本地基准 sha 存 `cache/github_last_sha.json`（首次检查 = 基准，之后 has_update = (远程 sha != 本地 sha)）。
  - `github_mark_update_done(sha)` 命令：用户点"我已更新"后把最新 sha 记为本地基准（不再提示有更新）。
  - DEFAULT_SETTINGS 新增 `github_proxy`（默认 `http://127.0.0.1:10809`，国内必须）。
- **前端**（LeftPanel.vue）：
  - "GitHub 代理地址"输入框（修改后自动保存 settings）+ "检查更新"按钮（loading 状态）。
  - 信息展示：首次/有更新/已是最新 三种状态色块；最新提交 message/作者/时间（zh-CN 本地化）/SHA（前 7 位）；如仓库有 release 显示 tag/发布时间/release notes（截断 800 字）。
  - 操作行：查看提交（直跳 commit URL）/提交历史/仓库主页/"我已更新"按钮（仅有更新时显示）。
  - App.vue 监听 `github_update_info` 与 `github_update_marked`（marked 后自动重新检查）。

### 5. 后端 ASMR 音声站 / EroMMDTube / Oreno3D 完整实现（之前已加，本次确认 + 命令注册）
- ASMR-100：API 多域名容灾（`api.asmr-200.com/asmr.one/asmr-100.com/asmr-300.com`）、JWT 365 天 + 密码自动重登、社团/标签/声优索引、收藏 PUT /api/review marked、音轨树 /api/tracks/{id}?v=2、直链 /api/media/download/{workId}/{fileId} 无签名、内置音频播放器（悬浮底部 + 连续自动播放 + 快进倒带 + 音量记忆）。
- EroMMDTube：与 Oreno3D 共用 `_oreno_*` 系列函数 + `ORENO_SITES[site_key]` 选择器映射；卡片 `a.main__list-link`、iwara 外链 `figure.show__figure>a`、列表排序 hot/favorites/latest/popularity。
- 命令注册：`oreno_*` / `asmr_*` 系列全部接入 `command_loop`，支持 `site_key` 参数切换 oreno3d/erommdtube。

### 6. 补丁.exe 更新打包机制（2026-08-28 新增）
- **背景**：旧 `update/` 文件夹方案已废弃。新方案 = `补丁.exe` 单文件，与本体同目录运行后覆盖更新并自删。
- **构成**：
  - `patch_runtime.py`：补丁运行时逻辑（Tkinter GUI 无黑框）。
  - `patch_runtime.spec`：PyInstaller spec，`datas=[('update', 'update')]` 把 update/ 文件夹整体内嵌进单文件 onefile exe；`console=False`（无黑框）；`onefile=True`（自解压到 `sys._MEIPASS` 临时目录）。
  - `打包补丁.bat`：双击调用 `pyinstaller patch_runtime.spec --noconfirm --clean`，产物 `dist/补丁.exe` 复制到根目录 `补丁.exe`。
- **运行流程**：
  1. 用户把 `补丁.exe` 复制到本体目录（与 `小小下载器.exe` / `小小下载器/` 文件夹同级）。
  2. 关闭正在运行的 `小小下载器.exe`。
  3. 双击 `补丁.exe` → Tkinter GUI 显示标题/提示/进度条/日志区/"开始应用补丁"按钮。
  4. 点按钮 → 遍历内嵌 `update/` 下所有文件 → 逐个 `shutil.copy2` 到本体目录同名相对路径（`Path(sys.executable).parent / rel`）。
  5. 跳过本体 `小小下载器.exe` 自身（被锁，本体 exe 更新走单文件 EXE 重新下载流程）。
  6. 失败项（如运行中的 exe 被占用）记录明细，让用户关闭后重试。
  7. 成功后 GUI 提示"3 秒后自动关闭并删除补丁程序"，调用 `self_delete()` → spawn `cmd /c "ping 127.0.0.1 -n 4 >nul & del /f 补丁.exe"`（CREATE_NO_WINDOW 隐藏窗口，3 秒延时等进程退出解锁文件后删除）。
- **打包时机**：改完代码同步 update/ 后双击 `打包补丁.bat` 重新生成。新单文件 EXE + 补丁.exe 双产出。
- **Tkinter 兼容**：开发模式（python patch_runtime.py）回退到 CLI fallback（有黑框，仅测试用）；打包后 onefile 模式默认走 GUI。



## 十一、文件速查

| 文件 | 作用 |
|---|---|
| `gui_bridge.py` | Python 后端桥（所有站点逻辑、命令分发） |
| `gui/src/App.vue` | 前端状态中枢（事件分发、命令发送、改名弹窗） |
| `gui/src/components/LeftPanel.vue` | 登录/账号卡片/设置/分类管理/历史收藏面板 |
| `gui/src/components/RightPanel.vue` | 搜索/浏览/关注列表/详情页/下载视图 |
| `gui/electron/main.cjs` | Electron 主进程（Python 子进程、悬浮窗、下载管理窗口） |
| `fetch_cookies.py` | 浏览器 Cookie 一键抓取 |
| `cache/twitter_follow_tags.json` | X 关注分类（母子 tag） |
| `cache/twitter_follows.json` | X 已归类关注 |

## 十二、核心代码逻辑速查

### 免安装 Python 分享（重要修复）
- `main.cjs` 的 `findPythonPath()` **最高优先级使用根目录 `bunkr_bridge/bunkr_bridge.exe`**（PyInstaller onedir 打包的后端），任何模式（dev/打包）都优先；找不到才回退系统 Python（逐个 `--version` 验证，过滤 WindowsApps stub）。
- 用内置 exe 时**不能追加 scriptPath**（`isBundledExe` 判断，exe 已内嵌 gui_bridge.py）。
- **修改 gui_bridge.py 后必须重新打包**：双击根目录 `打包后端.bat`（内部：`pyinstaller gui_bridge.spec` → 替换根目录 `bunkr_bridge/`）。
- **分享方式**：直接压缩整个根目录（需含 `bunkr_bridge/`、`gui/`（含 dist 与 node_modules 里的 electron）、`启动.bat`）；对方无需安装 Python。
- 报错特征（已修复）：对方机器只有 WindowsApps 的 python.exe stub → spawn 成功但 stderr "Python was not found"、退出码 9009。

### 前后端通信协议（NDJSON）
```
前端 sendCommand({cmd, ...参数})  → main.cjs 写入 stdin（UTF-8）
后端 read_command() 逐行解析 JSON → 命令分发 if/elif 链
后端 emit({"event": "...", ...数据}) 写 stdout → preload 转发 →
App.vue handlePythonEvent switch/case 分发到各状态
```
- 关键：`PYTHONIOENCODING=utf-8` 环境变量 + `sys.stdin/stdout.reconfigure(encoding='utf-8')`，否则中文命令乱码（GBK 默认）。
- Vue reactive（Proxy）对象过 IPC 前必须 `JSON.parse(JSON.stringify(obj))`。

### X (Twitter) API 调用核心
```python
# GraphQL（queryId 会轮换，404 时自动从 x.com 首页 main.js 刷新）
_twitter_api_get(qid, op, features, variables)
# v1.1 稳定端点（关注列表/关注操作，Bearer+Cookie+ct0）
_twitter_v11("GET", "friends/list.json", {"count": 200, "cursor": ...})
_twitter_v11("POST", "friendships/create.json", data={"user_id": ...})
# mutation（关注/取关回退路径）
_twitter_api_post(qid, op, features, variables)
```
- 请求节流：`_twitter_throttle()` 间隔 1.2s（防限流）。
- 缩略图：twimg 域名走代理缓存到 `cache/thumbnails/`，前端 `thumb://local/` 协议读取。

### X 浏览模式分批加载（本次新增）
```python
async def twitter_browse(offset=0):
    # 1. 取关注列表（优先用本地缓存）
    # 2. candidates = 有推文的博主[offset : offset+30]  ← 每批 30 人
    # 3. 逐人拉 UserMedia 最新页（节流 1.2s），每人取 2 条带媒体推文
    # 4. offset>0 时：新推文 + 旧缓存按 tweet_id 去重合并 → 时间倒序
    # 5. 缓存到 twitter_browse_cache.json（含 offset 进度），上限 600 条
    emit twitter_browse_feed {items, has_more, next_offset, append}
```
前端状态：`twBrowseHasMore` / `twBrowseNextOffset`，"点击继续更新"按钮 → `twitter_browse(offset=next_offset)`。

### X 本地搜索（纯前端过滤，本次新增）
```javascript
// App.vue handleTwLocalSearch(keyword)
// 1. 首次搜索：快照当前视图 {mode, items, feed} → twSearchSnapshot
// 2. 过滤范围：浏览信息流（text/昵称/@handle/链接）+ 用户列表（昵称/handle/简介/分类tag）
// 3. 关键词转小写，includes 匹配；结果分 user 卡片 + tweet 卡片两类
// 4. 清空/返回：从快照恢复原视图
```

### 主题切换核心（本次修复）
```javascript
// App.vue
const naiveTheme = computed(() => (settings.theme === 'light' ? null : darkTheme))
watchEffect(() => document.documentElement.classList.toggle('light-mode', settings.theme === 'light'))
```
```css
/* App.vue 全局样式（非 scoped），html.light-mode 选择器特异性压过组件暗色样式 */
html.light-mode .bottom-area { background: #ffffff; border-top-color: #e5e6eb; }
html.light-mode .round-btn { background: #ffffff; color: #5a5c66; }
```
要点：组件内 scoped 样式特异性不足以被覆盖时，统一在 App.vue 用 `html.light-mode .类名` 提升；naive-ui 组件本身跟随 `n-config-provider :theme` 自动切换。

### 下载去重核心
```python
# gui_bridge.py _resolve_duplicate(path, filename, expected_size, options)
# 1. 不存在 → 正常下载
# 2. skip_duplicates 且大小一致 → 跳过（file_complete 带 skipped 标记）
# 3. skip_duplicates 且大小不同：
#    manual_rename=True → emit download_rename_prompt（前端弹窗，rename_map 回传重下）
#    manual_rename=False → _unique_download_filename() 自动 "name (1).ext" 顺延
```

---

## 十三、更新记录  2026-08-28 深夜（v1.2.0：布局优化 + 翻译按钮重做 + EX批量下载修复 + JavDB 新站接入）

### 本次完成
1. **布局优化**：搜索按钮缩小 1/2；移除"选择中文"语言按钮（语言已并入左侧设置）。
2. **翻译按钮重做**：缩小 1/3；切换式（点击一次开始翻译当前页全部内容，再点一次停止）；翻译进行时转圈动效；修复点击不生效；移除"持续自动翻译"开关。
3. **EX 站批量下载修复**：搜索结果工具栏常驻"批量下载全部"按钮，点击后弹母文件夹命名弹窗（默认=当前搜索词，可留空），确认后逐画廊解析加入文件列表统一下载。
4. **JavDB（javdb.com）新站接入**（X 站类型 + webview 邮箱密码登录）：
   - 登录：左侧邮箱密码表单  webview 弹窗（登录页自动预填账号，Cloudflare 人机验证用户完成后点网站登录） 抓 cookie（含 cf_clearance + UA 绑定）加密保存，"记住装置"约 7 天有效；过期重新登录即可；支持登出。
   - 代理：`javdb_proxy` 设置（默认 http://127.0.0.1:10809，国内必须），登录 webview/搜索/下载全走代理。
   - 搜索：番号/标题/演员名，卡片显示封面/时长/字幕/VR角标/评分/日期；分页浏览。
   - 详情：点卡片进详情页封面大图 + 信息面板（番号/日期/时长等）+ 演员/标签 tag + 磁力链接列表（点击复制，含大小/日期/字幕标签）+ 预览图网格（点击看原图）；"下载图片"按钮下载封面+全部预览图（直链下载任务，走通用下载管理器带进度）。
   - 批量下载：搜索结果工具栏"批量下载全部"，与 EX 同套交互（母文件夹命名弹窗  每个视频一个下载任务，按 <母文件夹>/<番号>/ 归档）。
   - 链接解析：粘贴 javdb.com/v/{id} 链接直接进详情。
   - webview 会话：partition `persist:javdb`（独立于 X 站 OAuth）。

### 关键实现位置
- 后端 `gui_bridge.py`：`javdb_set_proxy / javdb_set_cookies / javdb_check_login / javdb_logout / javdb_search / javdb_video_info / javdb_download_images / javdb_batch_download / _javdb_download_one / is_javdb_url`（约 12158-12630 行）。
- 前端：`App.vue` javdb 状态与事件接线（javdbUser/javdbDetail/批量进度/母文件夹弹窗复用 batchFolderContext）；`LeftPanel.vue` javdb 登录表单/账号卡片/代理设置/帮助文本；`RightPanel.vue` javdb 搜索结果卡片/详情视图/批量下载按钮/样式。
- `WebviewLoginModal.vue`：新增 `credentials` prop，登录页加载完成后自动预填邮箱密码（javdb/xvideos 共用）。

### 剩余任务（后续继续）
1. **xhamster / pornhub / xvideos 三站业务接入**：目前只有登录框架（webview OAuth/邮箱密码登录已通），搜索/浏览/解析/下载待实现需要先调研三站的页面结构与直链规律（xhcdn/phncdn CDN），建议参考 JavDB 的 HTML 解析模板逐站补全 `_xhamster_soup` 等解析函数。
2. **JavDB 增强（可选）**：演员页/用户收藏/预览图大图翻页。
3. 验证 JavDB 真实登录流程（账号 837350217@qq.com 已预填逻辑，需用户实际走一次 Cloudflare 验证确认 cookie 抓取成功）。

### 产物（v1.2.0）
- 新单文件 EXE：`小小下载器/小小下载器 1.2.0.exe`
- 更新包：`update/`（gui_bridge.py + gui 源码 + dist + bunkr_bridge + 小小下载器 exe）
- bugfile/ 已同步

---

## 十四、更新记录  2026-08-28 深夜二（v1.2.1：EX批量下载修复 + GitHub检查改手动）

### 本次完成
1. **EX 批量下载修复（解析完成但没有下载任务）**：
   - 根因一（前端）：批量解析完成后文件只加进文件列表，从不自动提交下载  已改为解析全部完成后自动调用 handleDownload 提交下载任务（提示"解析完成，自动开始下载 N 个文件"）。
   - 根因二（后端）：DownloadManager._run 没有处理 batch_parent_folder（母文件夹逻辑只在旧 gui_download 里有，实际下载走的是 DownloadManager） 已补上：options.batch_parent_folder 非空时任务顶层目录 = 母文件夹名，EX 按 <母文件夹>/<画廊标题>/、JavDB 按 <母文件夹>/<番号>/ 归档。
2. **GitHub 更新检查改为纯手动**：
   - 根因：githubChecking 之前用 "结果为 null" 判断，启动后结果一直是 null  设置区永远显示"正在连接 GitHub..."（实际没发请求，误导为一直在获取）。
   - 修复：改为显式 loading 状态（仅手动点击"检查更新"时为 true）；启动后显示"未检查（点上方按钮手动获取）"；"我已更新"后不再自动复查 GitHub（只本地更新提示状态）。
3. **全站下载/预览/播放静态验证**：Iwara（视频播放走本地媒体代理 + 解析下载）、Hanime1（播放+批量下载）、Oreno3d（播放+iwara源下载）、ASMR（音频试听+批量）、JavDB（封面详情+图片下载+批量）、Bunkr/Coomer/Pawchive（共用文件列表下载）、EX（详情+批量已修）接线均正常。

### 剩余任务
1. xhamster/pornhub/xvideos 三站业务接入（登录框架已通，搜索/解析/下载待实现）。
2. 用户实际运行验证：EX 批量下载（应自动出现下载任务+按母文件夹归档）、各站播放/下载。
3. JavDB 真实登录流程验证（Cloudflare 验证 + cookie 抓取）。

### 产物（v1.2.1）
- 新单文件 EXE：`小小下载器/小小下载器 1.2.1.exe`（已移除 1.2.0）
- 更新包：`update/` + `补丁.exe`（已重新生成，内嵌本次全部改动）
- bugfile/ 已同步
